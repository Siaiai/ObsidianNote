---
area: "[[工作]]"
status: DOING
createTime: 2026-04-01 12:59:06
deadline: 2026-04-20T12:59
doneTime:
tags:
  - project
  - code
type: none
---

## 待办列表
- [ ] 编码
- [ ] 测试
- [ ] 合并
### 1.通信协议
#### 协议格式：
##### 约定
协议格式中的数据采用小端模式，
整帧最小长度 = 11 字节（无数据）
整帧最大长度 = 267字节（单帧满载）
##### 帧格式
| 名称       | 长度     | 内容      | 备注                         |
| -------- | ------ | ------- | -------------------------- |
| HEAD 帧头  | 1      | 0x55    | 固定帧起始                      |
| LEN 帧总长  | 2      | 整帧字节总数  | 包含 HEAD~TAIL 全部字段，**小端模式** |
| VER 版本   | 1      | 0x01    | 当前协议唯一版本                   |
| CFG 配置位  | 1      | 配置为     | 通信属性配置位                    |
| SEQ 序列号  | 2      | 0~65535 | 帧序列号，每帧递增，用于 ACK 匹配，**小端模式** |
| CMD 指令码  | 2      | 自定义指令   | 业务功能码，**小端模式**             |
| DATA 数据域 | LEN-11 | 业务数据    | 最小0字节，最大256字节              |
| CHECK 校验 | 1      | CRC8    | 校验范围：LEN ~ DATA            |
| TAIL 帧尾  | 1      | 0xAA    | 固定帧结束                      |

##### 配置位
| Bit7 | Bit6 | Bit5 | Bit4 | Bit3 | Bit2 | Bit1 | Bit0 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| 保留 | 保留 | 保留 | 保留 | 保留 | 保留 | 应答帧 | 是否应答 |

- **Bit0 是否应答（need_ACK）**：1 = 需要应答确认
- **Bit1 应答帧（is_ACK）**：1 = 本帧为应答帧
- **Bit2~7 保留**

> 分包机制不在协议库内实现，由应用层自行定义。单帧最大载荷 256 字节，超出部分由应用层自行拆分/重组，库仅按普通帧收发。

###  1.通信流程

```mermaid
sequenceDiagram
    participant 面板 as 操作面板
    participant BSP as 串口底层
    participant 驱动 as 协议驱动层
    participant 业务 as 业务层(回调)
    rect rgba(180, 240, 180, 0.3) # 外层绿色区块
	面板 ->> BSP: 发送指令
	BSP ->> 驱动: bspUsartGetFrame()获取数据
	note over 驱动: 解析
	驱动 ->> 业务: 触发指令回调
	note over 业务: 业务处理
	业务 ->> 驱动: 返回执行结果
	note over 驱动: 组装应答帧
	驱动 ->> BSP: bspUsartSend()发送数据
	BSP ->> 面板: 接收应答
	 end
```

### 2026/4/17面板功能会议

 -  不兼容升级，只烧录定制版本，分阶段，后续进行开发
 - [ ] 支持中/英文；
 - [ ] IP/端口设置界面：支持四段IP设置，端口选择
 - [ ] 模块开关界面：
 - [ ] 参数设置界面：
 - [ ] 通道/模块


```mermaid
sequenceDiagram
    participant A as 请求方A
    participant B as 接收方B
    alt 短请求 + 无回复
        A->>B: 请求单帧 (need_ACK)
        Note over B: 回调返 reply_len=0
        B-->>A: 纯ACK
        Note over A: 完成
    else 短请求 + 短回复
        A->>B: 请求单帧 (need_ACK)
        Note over B: 回调返短reply
        B->>A: 回复单帧 (need_ACK, 带reply)
        A-->>B: 纯ACK
        Note over A: reply_callback
    else 长请求 + 无回复
        A->>B: 请求分片 (need_ACK 逐片)
        B-->>A: 纯ACK (逐片)
        Note over B: 重组完, 回调返0
        B-->>A: 纯ACK (末片确认)
        Note over A: 完成
    else 短请求 + 长回复
        A->>B: 请求单帧 (need_ACK)
        B-->>A: 纯ACK
        Note over B: 回调返长reply
        B->>A: 回复分片 (need_ACK 逐片)
        A-->>B: 纯ACK (逐片)
        Note over A: reply_callback
    else 长请求 + 短回复
        A->>B: 请求分片 (need_ACK 逐片)
        B-->>A: 纯ACK (逐片)
        Note over B: 重组完, 回调返短reply
        B->>A: 回复单帧 (need_ACK)
        A-->>B: 纯ACK
        Note over A: reply_callback
    end
```

