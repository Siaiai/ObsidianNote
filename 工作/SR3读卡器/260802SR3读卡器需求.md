---
领域: 工作
类型:
  - 开发
项目:
  - SR3读卡器
任务:
  - "[[260802SR3读卡器需求]]"
相关任务:
  - "[[260802SR3读卡器架构]],[[260802SR2读卡器行为]]"
状态: 进行
创建时间: 2026-08-02T02:57:17
---

# 原SR3 读卡器 —— 行为规范（HC32F002）

> 源码：`HC32F002_DDL_Rev1.2.0（卡号错误问题解决优化版）\midware\RFID\RFID.c` 等
> MCU：HC32F002，48MHz RCH，Flash 18KB，RAM 2KB
> 用途：新 SR3 重构时的功能参考基线

---

# 一、通信协议

## 1.1 物理层

- UART 19200-8-N-1，LPUART1
- 48MHz RCH → PCLK 6MHz（`SYSCTRL_SYSCLK_PCLK_PRS8`）
- 帧头固定 0x10 0xFF，数据长度由 CMD 决定

## 1.2 帧格式

```
┌────────┬────────┬────────┬────────────────────┬─────────┐
│  0x10  │  0xFF  │  CMD   │  DATA（变长）        │  BCC?   │
└────────┴────────┴────────┴────────────────────┴─────────┘
```

- CMD 决定 DATA 长度，不在帧中传 LEN
- 模式 2/3 时 BCC 附加在末尾：`BCC = XOR(0x10, 0xFF, CMD, DATA[0..n])`

## 1.3 接收指令（上位机 → 读卡器）

| 字节序列 | 语义 | S2控制器 | S3控制器 |
|---|---|---|---|
| `10 FF 7A` | 进站查询 + 切模式1 + 设进站方向 | ✅ | ✅ |
| `10 FF 75` | 下放查询 + 切模式1 + 设下放方向 | ✅ | ✅ |
| `10 FF 9A` | 进站 + 切模式2（BCC）+ 设进站方向 | ❌ | ❌ |
| `10 FF 95` | 下放 + 切模式2（BCC）+ 设下放方向 | ❌ | ❌ |
| `10 FF 9D` | 切模式3（连续上报） | ❌ | ❌ |
| `10 FF 90` | 查询版本号 | ❌ | ❌ |
| `10 A0 05` | 系统复位（进 Bootloader） | ❌ | ✅ |

> S2/S3 控制器**从未使用过** 0x9A/0x95/0x9D/0x90 四条命令。它们是读卡器固件预留的。S3 控制器只用了 0x7A、0x75、0xA0 0x05 三条。

SR3 命令解析在主循环 `Read_Mode_Change()` 中，不依赖 UART 中断。0xA0 0x05 命令**不校验 buf[0] 是否为 0x10**，只要 `buf[1]==0xA0 && buf[2]==0x05` 即调用 `NVIC_SystemReset()`。

## 1.4 发送指令（读卡器 → 上位机）

| 字节序列 | 语义 | 模式 |
|---|---|---|
| `10 FF 05 + 5字节卡号` | 卡号包，8字节 | 1 |
| `10 FF 04 46 61 69 6C` | Fail包，7字节 | 1 |
| `10 FF 05 + 5字节卡号 + BCC` | 卡号包带校验，9字节 | 2/3 |
| `10 FF 04 46 61 69 6C + BCC` | Fail包带校验，8字节 | 2/3 |
| `10 FF 01 56 65 72 30 33 31 30` | 版本包 "Ver0310"，10字节 | 任意 |

## 1.5 忙保护

收到指令时，满足以下任一条件则**静默忽略**：
- 串口正在发送
- 进站 100ms 等待期或 50ms 冷却期

**忙保护只阻止指令应答，不阻止新卡上报。** 新卡检测条件独立于忙状态。

---

# 二、读卡核心

## 2.1 上电启动

```mermaid
flowchart TD
    PowerOn(["上电"]) --> Clock["48MHz RCH → PCLK=6MHz"]
    Clock --> GTIM["GTIM: ~125kHz 方波"]
    Clock --> ATIM3["ATIM3: 50ms 节拍中断"]
    Clock --> UART["LPUART1 19200-8-N-1"]
    GTIM --> LED["两灯全灭"]
    ATIM3 --> LED
    UART --> LED
    LED --> EM4095["EM4095: SHD=1→50ms→SHD=0"]
    EM4095 --> IWDT["启动看门狗 ~3.2s"]
    IWDT --> RemoteUpdate["RemoteUpdateInit()"]
    RemoteUpdate --> MainLoop["进入主循环"]
```

> 与 SR2 不同：SR3 上电**两灯全灭**，SR2 上电红灯亮。

## 2.2 主循环

```mermaid
flowchart TD
    Loop(("while(1) 有看门狗")) --> Cmd["Read_Mode_Change()<br/>命令处理"]
    Cmd --> LED["LED_Deal()<br/>LED 刷新"]
    LED --> Decode["RFID_Decode_125Khz()<br/>曼彻斯特解码"]
    Decode --> Check["RFID_check()<br/>停止位+行+列校验"]
    Check --> Process["RFID_process()<br/>卡号提取+变化检测+触发上报"]
    Process --> Send["RFID_Send_Handler()<br/>二次发送/连续发送"]
    Send --> Feed["IWDT_Feed()<br/>喂狗"]
    Feed --> Loop
```

## 2.3 曼彻斯特解码

GTIM 中断（~125kHz）检测 DEMOD_OUT(PC05) 跳变→记录脉宽→主循环分类解码：

| 脉宽 | 阈值 |
|---|---|
| 窄 (~1bit) | 20~41 |
| 宽 (~2bit) | 41~82 |
| 超出范围 | 解码复位 |

- 64 位收齐 → `decode_end_flag=1`
- 卡头保护：前 9 位出现 0 → 复位
- 校验期间关闭 GTIM（125kHz 停振），校验完恢复
- 解码失败时调用 `Reset_RFID_Decode()` 复位全部解码状态

## 2.4 校验：三关

| 关 | 方法 | 说明 |
|---|---|---|
| 停止位 | `decode_data[63] != 0` → 失败 | **SR3 新增**，SR2 没有 |
| 行校验 | 10 行奇校验 | 同 SR2 |
| 列校验 | 4 列奇校验 | 同 SR2 |

三关全过 → `Data_check_flag=1`。失败时 `Reset_RFID_Decode()` 复位解码器。

## 2.5 卡号提取与变化检测

```mermaid
flowchart TD
    Entry("校验通过") --> Extract["64位 → 5字节卡号<br/>buffer[3..7] 大端"]
    Extract --> NotZero{"卡号非全零?"}
    NotZero -->|"否"| Off["绿灯灭"]
    NotZero -->|"是"| Compare{"buffer[4..7]<br/>与上次相同?"}
    Compare -->|"不同"| New["标记新卡 + 更新基准<br/>同时预计算 BCC"]
    Compare -->|"相同"| Skip["跳过, 同卡去重"]
    New --> Trigger["触发上报"]
```

> 与 SR2 一致：只比较 buffer[4..7]（卡 ID），buffer[3] 不参与。SR3 区别：新卡时同时预计算 BCC。

## 2.6 主动上报触发条件

```c
if(Read_card_success_flag==0 || Kahao_new_flag!=0)
```

- 当前无卡（200ms 超时清零）→ 触发
- 检测到新卡 → 触发
- **不检查 100ms_flag**——即使在等待/冷却中也会触发

---

# 三、位置行为

## 3.1 进站（Send_Manner = 0x55）

指令 0x7A 或 0x9A 设置为进站方向。

```mermaid
sequenceDiagram
    participant 标签
    participant 读卡器
    participant 上位机

    标签->>读卡器: 进入感应区
    读卡器->>读卡器: 解码: 卡号=0xAABBCCDDEE

    rect rgb(255, 235, 238)
        Note over 读卡器: 🔴 触发立即上报
        读卡器->>上位机: 第1次: 0x10 0xFF 0x05 AA BB CC DD EE
    end

    rect rgb(255, 243, 224)
        Note over 读卡器: 🟠 100ms 等待
        Note over 读卡器: ...100ms 后...
        读卡器->>上位机: 第2次: 0x10 0xFF 0x05 AA BB CC DD EE（同一卡号）
        Note over 读卡器: 🟡 50ms 冷却<br/>期间忽略上位机指令
    end

    rect rgb(232, 245, 233)
        Note over 读卡器: 🟢 冷却结束, 恢复正常
        上位机->>读卡器: 0x7A (进站轮询)
        读卡器->>上位机: Fail 7字节 (心跳)
    end
```

**时序参数：** 100ms（ATIM3 50ms × 2 tick），50ms 冷却（× 1 tick）。模式 3 时不进冷却。

## 3.2 下放（Send_Manner = 0xAA）

指令 0x75 或 0x95 设置为下放方向。

```mermaid
sequenceDiagram
    participant 标签
    participant 读卡器
    participant 上位机

    标签->>读卡器: 进入感应区
    读卡器->>读卡器: 解码: 卡号=0xAABBCCDDEE

    rect rgb(255, 235, 238)
        Note over 读卡器: 🔴 触发立即上报
        读卡器->>上位机: 仅1次: 0x10 0xFF 0x05 AA BB CC DD EE
    end

    rect rgb(232, 245, 233)
        Note over 读卡器: 🟢 无二次发送, 无冷却
        上位机->>读卡器: 0x75 (下放轮询)
        读卡器->>上位机: 有卡 → 卡号, 无卡 → Fail
    end
```

## 3.3 进站 vs 下放 对比

```mermaid
flowchart LR
    subgraph 进站["进站 (0x7A/0x9A)"]
        E1["新卡 → 立即上报"]
        E2["100ms 后二次上报"]
        E3["50ms 冷却(忽略指令)"]
        E4["0x7A 轮询 → 只答 Fail"]
    end

    subgraph 下放["下放 (0x75/0x95)"]
        R1["新卡 → 立即上报"]
        R2["无二次上报"]
        R3["无冷却"]
        R4["0x75 轮询 → 有卡报卡号<br/>无卡报 Fail"]
    end

    style E2 fill:#FF9800,color:#fff
    style E3 fill:#FFC107,color:#000
    style R2 fill:#999,color:#fff
    style R3 fill:#999,color:#fff
```

## 3.4 轮询应答规则

| 指令 | 忙时 | 空闲时 |
|---|---|---|
| 0x7A（进站） | 静默忽略 | **只应答 Fail**，不报卡号 |
| 0x75（下放） | 静默忽略 | **有卡报卡号，无卡报 Fail** |
| 0x9A（进站+BCC） | 静默忽略 | 同 0x7A，但应答含 BCC，切模式2 |
| 0x95（下放+BCC） | 静默忽略 | 同 0x75，但应答含 BCC，切模式2 |
| 0x9D（连续模式） | - | 切模式3，无应答 |
| 0x90（查版本） | - | 直接回版本号 10 字节 |

## 3.5 新卡打断规则

新卡优先级最高，随时打断任何流程。触发条件不检查 100ms_flag。

```mermaid
sequenceDiagram
    participant 标签A as 标签A
    participant 标签B as 标签B
    participant 读卡器
    participant 上位机

    标签A->>读卡器: 进入感应区
    读卡器->>上位机: 卡A 第1次上报
    Note over 读卡器: 100ms 等待中...

    Note over 读卡器: 50ms 时...
    标签B->>读卡器: 进入感应区 (新卡!)
    读卡器->>上位机: 卡B 第1次上报
    Note over 读卡器: 100ms 重新计时<br/>卡A 二次发送被取消
```

| 场景 | 结果 |
|---|---|
| 100ms 等待中来新卡 | 立即发新卡，原卡二次发送取消 |
| 50ms 冷却中来新卡 | 立即发新卡，重新开始 100ms 计时 |
| 发送中来新卡 | `delay1ms(20)` 等 TX 空闲 → 发新卡 |

## 3.6 三种模式

```mermaid
flowchart TB
    subgraph M1["模式 1 —— 默认"]
        M1_1["新卡立即发<br/>进站二次发 + 50ms冷却"]
        M1_2["无 BCC"]
        M1_3["绿灯=有卡 · 红灯=绿灯取反"]
        M1_4["响应 0x7A / 0x75"]
    end

    subgraph M2["模式 2 —— 加校验"]
        M2_1["行为同模式1"]
        M2_2["所有包末尾加 BCC"]
        M2_3["LED 切换闪烁 4 次(vs 2次)"]
        M2_4["响应 0x9A / 0x95"]
    end

    subgraph M3["模式 3 —— 连续发"]
        M3_1["不等新卡事件, 定时器驱动"]
        M3_2["有卡每 100ms 发, 无卡每 500ms 发"]
        M3_3["红灯常亮"]
        M3_4["不处理轮询, 0x7A/0x75 退出"]
    end

    M1 -->|"0x9A/0x95"| M2
    M2 -->|"0x7A/0x75"| M1
    M1 -->|"0x9D"| M3
    M2 -->|"0x9D"| M3
    M3 -->|"0x7A/0x75/0x9A/0x95"| M1
```

| 维度 | 模式 1 | 模式 2 | 模式 3 |
|---|---|---|---|
| 进入指令 | 0x7A / 0x75 | 0x9A / 0x95 | 0x9D |
| 上报驱动 | 新卡事件 | 新卡事件 | 定时器 |
| 进站二次发 | ✅ | ✅ | 不适用 |
| 50ms 冷却 | ✅ | ✅ | ❌ |
| BCC | ❌ | ✅ | ✅ |
| 有卡频率 | 1 次（+进站二次） | 1 次（+进站二次） | 每 100ms |
| 无卡频率 | 不发 | 不发 | 每 500ms |
| 红灯 | 绿灯取反 | 绿灯取反 | 常亮 |

### 模式 3 的工作方式

```mermaid
sequenceDiagram
    participant 定时器 as ⏱️ 50ms 定时器
    participant 读卡器
    participant 上位机

    Note over 读卡器: 进入模式 3

    rect rgb(232, 245, 233)
        Note over 读卡器: 有卡时:
        定时器->>读卡器: 100ms (2 tick)
        读卡器->>上位机: 卡号 9字节 (含BCC)
        定时器->>读卡器: 又 100ms
        读卡器->>上位机: 卡号 9字节 (含BCC)
    end

    rect rgb(255, 243, 224)
        Note over 读卡器: 卡离开了:
        定时器->>读卡器: 250ms (5 tick) → 绿灯灭
        定时器->>读卡器: 500ms (10 tick)
        读卡器->>上位机: Fail 8字节 (含BCC)
        定时器->>读卡器: 又 500ms
        读卡器->>上位机: Fail 8字节 (含BCC)
    end
```

### 模式切换时 LED 闪烁

切换到新模式时，绿灯快速闪烁确认：
- 模式 1：2 次（`temp_led_num_flag=2`）
- 模式 2：4 次（`temp_led_num_flag=4`）
- 模式 3：6 次（`temp_led_num_flag=6`）

每次闪烁约 200ms（ATIM3 50ms × 4 tick）。

## 3.7 200ms 无卡超时

无新卡解码超过 200ms → `Read_card_success_flag=0` → 绿灯灭。ATIM3 50ms × 4 tick。

---

# 四、LED 行为

## 4.1 引脚与初始态

- PD02 绿灯，PD03 红灯，低电平亮
- 上电初始：**两灯全灭**（与 SR2 上电红灯亮不同）

## 4.2 模式 1/2 常态

```mermaid
flowchart LR
    Normal["正常运行"] --> HasCard{"有卡?"}
    HasCard -->|"是"| GreenOn["绿灯亮 · 红灯灭"]
    HasCard -->|"否"| GreenOff["绿灯灭 · 红灯亮"]
    GreenOff --> HasCard
    GreenOn --> HasCard
```

红灯 = 绿灯取反（`GPIO_PD02_READ()` 取反后设红灯）。

## 4.3 模式 3

- 红灯**常亮**（不参与取反）
- 有卡 → 绿灯亮
- 无卡 250ms（5 tick × 50ms）→ 绿灯灭

## 4.4 黑卡时序

卡号后 3 字节为 `00 00 5A` 时触发：

```mermaid
sequenceDiagram
    participant 读卡器
    participant LED

    读卡器->>LED: 检测到黑卡
    Note over LED: ① 两灯全灭
    Note over LED: ② 等待 300ms
    Note over LED: ③ 两灯同时亮
```

用于标识特定设备/推车。

---

# 五、固件升级

## 5.1 升级架构

```mermaid
flowchart LR
    PC["🖥️ PC<br/>UDP 下发固件"] -->|"UDP"| S3["🖥️ S3控制器<br/>存W25QXX, 编排升级"]
    S3 -->|"UART 0x55帧"| BL["🔧 Bootloader<br/>擦/写/解密/跳转"]
    BL --> Flash["💾 内部Flash 18KB"]

    style PC fill:#f3e5f5
    style S3 fill:#e3f2fd
    style BL fill:#fff3e0
```

三端协作：PC 下发 → S3 编排 → Bootloader 执行。读卡器 App 在升级中角色极小：只负责上电上报结果 + 接 0xA0 0x05 复位。

## 5.2 Flash 布局

```
0x0000 ┌──────────────┐
       │  Bootloader  │  7KB (0x0000~0x1BFF)，永不被擦
0x1C00 ├──────────────┤
       │ Application  │  10KB (0x2A00)，最大 42 包×256B
0x4600 ├──────────────┤
       │  参数区       │  512B，App↔Bootloader 共享
0x4800 └──────────────┘  Flash 总 18KB
```

**UpdateFlg（@0x4604，Flash 持久化）——App 和 Bootloader 的唯一握手渠道：**

| 值 | 含义 | Bootloader 动作 |
|---|---|---|
| 0xFF | 擦除态/正常 | 立即跳 App |
| 0 (rNormal) | 正常 | 驻留监听 |
| 5 (rRecive) | 正在收固件 | 驻留监听 |
| 3 (rUpdated) | 升级完成 | App 见后发 0x03 上报 |

## 5.3 触发升级

```mermaid
sequenceDiagram
    participant S3 as 🖥️ S3 控制器
    participant App as Application
    participant BL as Bootloader

    Note over S3,App: 正常工作中
    S3->>App: 0x10 0xA0 0x05 (复位)
    App->>BL: NVIC_SystemReset()

    Note over BL: MCU 复位, Bootloader 先跑
    BL->>S3: CMD 0x04 (启动通知)
    BL->>BL: 读 UpdateFlg → 0xFF → 正常
    BL->>BL: 准备跳 App...

    rect rgb(255, 235, 238)
        Note over S3,BL: 🔴 S3 必须在跳走前抢发
        S3->>BL: CMD 0x81: PackedNum=42
        Note over BL: 收到 0x55 帧 → 放弃跳转
    end
```

> App 收 0xA0 0x05 后**直接复位，不预置 UpdateFlg**。靠 Bootloader 启动时 100ms 延时 + S3 抢发 0x81 来抓住窗口。

## 5.4 0x55 升级帧

```
上位机 → 读卡器（含 PackNo）:
┌──────┬─────┬──────────┬──────────┬──────────────┬──────────┬──────┐
│ 0x55 │ CMD │ Len(LE)  │ PackNo   │ Data[Len]    │ CRC16 LE │ 0xAA │
└──────┴─────┴──────────┴──────────┴──────────────┴──────────┴──────┘

读卡器 → 上位机（不含 PackNo）:
┌──────┬─────┬──────────┬──────────────┬──────────┬──────┐
│ 0x55 │ CMD │ Len(LE)  │ Data[Len]    │ CRC16 LE │ 0xAA │
└──────┴─────┴──────────┴──────────────┴──────────┴──────┘
```

## 5.5 升级命令

| CMD | 方向 | 干什么 |
|---|---|---|
| 0x81 | 上位机→BL | 启动升级（告知总包数，擦除 App 区） |
| 0x82 | 上位机→BL | 发送一包数据（256B + CRC16） |
| 0x83 | 上位机→BL | 查询升级结果 |
| 0x01 | BL→上位机 | 启动确认 |
| 0x02 | BL→上位机 | 数据包 ACK（uOK=0xAA / uFail=0x55） |
| 0x03 | App→上位机 | 升级结果报告（版本 + 成功/失败） |
| 0x04 | BL→上位机 | Bootloader 启动通知（4 字节零） |

## 5.6 完整升级流程

```mermaid
sequenceDiagram
    participant PC as 🖧 PC
    participant S3 as 🖥️ S3 控制器
    participant App as 📱 读卡器 App
    participant BL as 🔧 Bootloader
    participant W25 as 💾 W25QXX
    participant FL as 💾 Flash

    Note over PC,S3: ═══ 阶段0: PC→S3 下发固件 ═══
    PC->>S3: CMD_REMOTE_UPDATE (PackedNum, Target)
    S3->>W25: 擦除 0x280000 区
    loop 分包
        PC->>S3: CMD_UPDATE_FILE (PackNo+256B+CRC16)
        S3->>W25: 写 W25QXX
    end

    Note over S3,App: ═══ 阶段1: 进 Bootloader (sUpdate) ═══
    S3->>App: 0x10 0xA0 0x05
    App->>App: NVIC_SystemReset()
    BL->>S3: CMD 0x04 (alive)
    S3->>S3: State=sErase

    Note over S3,BL: ═══ 阶段2: 擦除 (sErase) ═══
    S3->>BL: CMD 0x81, PackNo=总包数
    BL->>FL: 擦 21 扇区 0x1C00~0x45FF
    BL->>FL: 写 UpdateFlg=rRecive
    BL->>S3: CMD 0x01 (确认)
    S3->>S3: State=sRecive, PackNo=0

    Note over S3,BL: ═══ 阶段3: 传固件 (sRecive) ═══
    loop 每包 1..N
        S3->>W25: 读 256B
        S3->>BL: CMD 0x82, PackNo, Data[256], CRC16
        BL->>BL: 序号校验 + CRC16 + XOR 解密
        BL->>FL: 写 0x1C00+(PackNo-1)×256
        BL->>S3: CMD 0x02, PackNo, uOK
        S3->>S3: PackNo++
    end

    Note over BL: ═══ 阶段4: 完成 ═══
    BL->>BL: 检查 GetPackFlag[1..N] 全 1
    BL->>FL: 写 UpdateFlg=rUpdated
    BL->>App: iap_load_app(0x1C00)

    Note over App: ═══ 阶段5: 新 App 上报 ═══
    App->>App: RemoteUpdateInit() 见 rUpdated
    App->>S3: CMD 0x03, 版本, uOK
    App->>FL: 擦 0x4600 (UpdateFlg→0xFF)
    S3->>PC: 上报升级结果
    Note over PC,S3: ✅ 升级完成
```

## 5.7 S3 控制器升级状态机

```mermaid
flowchart TD
    Trig["PC 发升级指令"] --> G0{"主轨道运行中?<br/>Len<3? PackedNum≥2048?"}
    G0 -->|"是"| Skip["静默跳过"]
    G0 -->|"否"| EraseW["擦 W25QXX → sUpdate"]

    EraseW --> SU["发 0x10 0xA0 0x05<br/>等 30ms, 重发"]
    SU --> SUr{"收到 0x04?"}
    SUr -->|"是"| SE["sErase → 发 0x81"]
    SUr -->|"5次无响应"| SENf["⚠️ 不失败!<br/>强制转 sErase"]
    SENf --> SE

    SE --> SEw["前3次30ms, 后2次3s"]
    SEw --> SEr{"收到 0x01?"}
    SEr -->|"是"| SR["sRecive, PackNo=0"]
    SEr -->|"5次无响应"| FAIL["uFail"]

    SR --> SRs["读W25→发0x82→等3s"]
    SRs --> SRr{"收到 0x02+uOK?"}
    SRr -->|"是"| SRn{"最后一包?"}
    SRn -->|"否"| SRnext["PackNo++ → 下一包"]
    SRnext --> SRs
    SRn -->|"是"| SW["sWaitRes → 等3s"]
    SRr -->|"5次失败"| FAIL

    SW --> SWr{"收到 0x03?"}
    SWr -->|"是"| ACK["回 0x83 → 上报PC → 完成"]
    SWr -->|"超时"| FAIL

    FAIL -.-> OT["整体超时 30s"]

    style Skip fill:#999,color:#fff
    style FAIL fill:#D94A4A,color:#fff
    style ACK fill:#50B86C,color:#fff
```

## 5.8 安全机制

| 机制 | 参数 |
|---|---|
| CRC | CRC16-USB (0x8005)，初值/结果 XOR 0xFFFF |
| 加密 | XOR，密钥 "603337"（6 字节循环），仅 Bootloader 解密 |
| 包序号 | 严格递增，跳跃则静默丢弃 |
| 重传 | S3 侧最多 5 次，Bootloader 无重传逻辑 |
| 超时 | S3: 30s 整体 + 3s 单步 / BL: ~4s（8000×500μs） |
| 栈校验 | 跳转前验证 `(*(app_addr) & 0x2FFE0000) == 0x20000000` |

## 5.9 失败后能不能恢复

```mermaid
flowchart TD
    Fail["升级失败"] --> Q{"什么情况?"}
    Q -->|"App 损坏跑飞"| WD["看门狗 3.2s 复位 → 回 Bootloader"]
    Q -->|"参数区异常"| Stay["Bootloader 驻留监听"]
    Q -->|"Flash 写坏"| Stay

    WD --> Boot["Bootloader 启动"]
    Stay --> Boot

    Boot --> Wait["等 S3 重新发起升级"]
    Wait --> ReUp["重来一次 → 恢复"]

    style ReUp fill:#50B86C,color:#fff
```

**结论：OTA 不会变砖。** 三道兜底：
1. Bootloader 区（0x0000~0x1BFF）永不被擦写
2. App 看门狗 ~3.2s 超时复位
3. UpdateFlg≠0xFF 时 Bootloader 驻留，0x81 来者不拒

但恢复依赖 PC/S3 重新发起升级，停留在 Bootloader 期间不读卡不上报，现场表现为"死机"。

## 5.10 升级代码位置

| 角色 | 文件 | 关键函数 |
|---|---|---|
| S3 编排 | `APP\RemoteUpdate.c` | `GotoUpdate()`, `UpdateFileRecv()`, `RemoteUpdate()` |
| 读卡器 App | `midware\RFID\RFID.c` | `Read_Mode_Change()` 接复位 |
| 读卡器 App | `midware\REMOTEUPDATE\RemoteUpdate.c` | `RemoteUpdateInit()` 上报结果 |
| Bootloader 主 | `source\main.c` | 启动决策 + 跳转 |
| Bootloader 升级 | `midware\REMOTEUPDATE\RemoteUpdate.c` | `GotoUpdate()`, `UpdateFileRecv()`, `iap_load_app()` |
| Bootloader 解帧 | `midware\CMDPROTOCOL\CmdProtocol.c` | `UPD_RFID_Prase()` |

---

# 六、与 SR2 的兼容性

## 6.1 兼容基线

上位机只发 0x7A 和 0x75 时，SR3 与 SR2 行为完全一致，可互换。SR3 收到 0x7A/0x75 会自动切回模式 1，向下兼容无感知。

## 6.2 差异总览

| 维度 | SR2 (STC15) | SR3 (HC32F002) |
|---|---|---|
| MCU | STC15W4K32S4 (32KB) | HC32F002 (18KB) |
| 命令数 | 2 条 | 7 条 |
| 模式 | 仅模式 1 | 模式 1/2/3 |
| BCC | ❌ | 模式 2/3 ✅ |
| 校验 | 行+列 (2关) | 停止位+行+列 (3关) |
| LED | 上电红灯亮，有卡绿灯亮 | 上电两灯灭，红绿互斥/闪烁/黑卡 |
| 看门狗 | ❌ | ✅ ~3.2s |
| 节拍 | Timer0 10ms | ATIM3 50ms |
| 发送 | TI 中断逐字节 | LPUART_Transmit 阻塞 |
| Bootloader | ❌ | ✅ 0x55 帧协议 |
| 远程升级 | ❌ | ✅ |