---
领域: 工作
类型:
  - 开发
项目:
  - "SR3读卡器"
任务:
  - "[[260802SR3读卡器需求]]"
相关任务:
  - "[[260802SR3读卡器架构]]"
状态: 进行
创建时间: 2026-08-02T02:57:17
截止时间: 2026-08-02T23:59
完成时间: 
---

## 待办列表
- [ ] 编码
- [ ] 测试
- [ ] 合并

---

# SR3 读卡器 · 完整功能需求文档

> 以 **读卡器为主视角**，回答三个问题：我能做什么？谁会跟我说话、说什么？我说出去之后会发生什么？
> 整合自 HC32F002 读卡器固件 + S2 控制器 + S3 控制器 三个工程。

---

## 一、我是谁

我是 **125kHz RFID 读卡器**，装在服装吊挂输送线上。我的工作就是：**一直等着衣架过来 → 读出衣架上的 RFID 标签 → 把卡号告诉上位机（控制器）**。

```mermaid
flowchart LR
    A[我是读卡器] --> B[我发 125kHz 载波]
    B --> C[衣架标签进入]
    C --> D[我解码 64 位曼彻斯特数据]
    D --> E[五道校验通过]
    E --> F[等上位机问我 -> 我答卡号]

    style A fill:#50B86C,color:#fff,stroke:#2d6a4f
    style F fill:#4A90D9,color:#fff
```

**我的硬件不变，核心就是三件事：**
- 产生 125kHz 载波（通过 EM4095 芯片）
- 解码 EM4100/EM4102 标签
- 通过 UART 串口（19200-8-N-1）和上位机说话

---

## 二、我能识别什么标签、怎么识别

### 2.1 标签规格

| 项目 | 规格 |
|------|------|
| 频率 | 125kHz |
| 标签协议 | EM4100 / EM4102 (EM4001 族) |
| 编码方式 | 曼彻斯特编码 (Manchester) |
| 原始数据结构 | 64 位 |

### 2.2 64 位数据的结构

```mermaid
flowchart LR
    H[9位头部 : 全为 1] --> D[40位数据 : 10行x4位,含行校验]
    D --> C[4位列校验]
    C --> S[1位停止位 : 必须为 0]

    style H fill:#4A90D9,color:#fff
    style D fill:#50B86C,color:#fff
    style C fill:#E8A838,color:#fff
    style S fill:#D94A4A,color:#fff
```

### 2.3 我的解码 = 五道关卡

```mermaid
flowchart TD
    Raw[64位原始数据] --> C1{第1关: 头部校验<br/>前9位 = 全1 ?}
    C1 -->|不通过| Trash[丢弃,重来]
    C1 -->|通过| C2{第2关: 行校验<br/>10行,每行5位奇校验 ?}
    C2 -->|不通过| Trash
    C2 -->|通过| C3{第3关: 列校验<br/>4列,每列11位奇校验 ?}
    C3 -->|不通过| Trash
    C3 -->|通过| C4{第4关: 停止位<br/>第64位 = 0 ?}
    C4 -->|不通过| Trash
    C4 -->|通过| C5{第5关: 非全零<br/>5字节不是全零 ?}
    C5 -->|全零| Trash
    C5 -->|通过| Done[有效卡号: 5字节]

    style C1 fill:#4A90D9,color:#fff
    style C2 fill:#50B86C,color:#fff
    style C3 fill:#E8A838,color:#fff
    style C4 fill:#D94A4A,color:#fff
    style C5 fill:#8E44AD,color:#fff
    style Done fill:#50B86C,color:#fff
    style Trash fill:#999,color:#fff
```

### 2.4 我是怎么从信号里读出比特的

```
我盯着 DEMOD_OUT 引脚的电平跳变，测量两次跳变的间隔（脉宽）：

  窄脉冲: 约 160 ~ 328 us  ->  记作类型1
  宽脉冲: 约 328 ~ 656 us  ->  记作类型2
  超出范围 ->  不认识了，从头再来

根据曼彻斯特规则从脉冲序列还原出 64 个比特。
```

---

## 三、我的三种工作模式

上位机发不同的命令，我就切到不同的模式：

```mermaid
flowchart TD
    Start([上电]) --> Mode1

    Mode1[模式1 默认模式] --> Send1A[读到卡 -> 立即上报 8字节]
    Send1A --> Send1B[100ms后 二次上报]
    Send1B --> Cool1[50ms冷却]
    Cool1 --> Idle1[等待读卡]
    Idle1 --> Send1A

    Mode2[模式2 校验模式] --> Send2A[读到卡 -> 立即上报 9字节+BCC]
    Send2A --> Send2B[100ms后 二次上报]
    Send2B --> Cool2[50ms冷却]
    Cool2 --> Idle2[等待读卡]
    Idle2 --> Send2A

    Mode3[模式3 连续模式] --> HasCard[有卡:每100ms上报]
    HasCard --> NoCard[无卡:每500ms报Fail]
    NoCard --> HasCard

    Mode1 -->|0x9A或0x95| Mode2
    Mode2 -->|0x7A或0x75| Mode1
    Mode1 -->|0x9D| Mode3
    Mode2 -->|0x9D| Mode3
    Mode3 -->|0x7A或0x75| Mode1

    style Mode1 fill:#4A90D9,color:#fff
    style Mode2 fill:#50B86C,color:#fff
    style Mode3 fill:#E8A838,color:#fff
```

| 模式 | 触发指令 | 上报格式 | 上报节奏 | 特点 |
|------|---------|---------|---------|------|
| 模式1 默认 | `0x7A`(进站) / `0x75`(下放) | 8字节 | 立即+100ms重复+50ms冷却 | 防丢包 |
| 模式2 校验 | `0x9A`(进站校验) / `0x95`(下放校验) | 9字节(末尾+1字节BCC) | 同模式1 | XOR校验 |
| 模式3 连续 | `0x9D` | 8字节 | 有卡每100ms / 无卡每500ms报Fail | 不等待轮询,红灯常亮 |

> **模式说明：** 模式1/2 为轮询应答模式（上位机问我才答），模式3 为主动上报模式（我不等上位机问就自己报）。方向标识（进站=0x55, 下放=0xAA）仅为内部状态，不影响协议帧格式。BCC = 对数据字节依次做 XOR。

---

## 四、我和上位机的对话协议（UART）

### 4.1 物理层

**19200 bps, 8 数据位, 1 停止位, 无校验, 无流控。**

上位机发 3 个字节问我，我回 7~10 个字节答它。

```mermaid
flowchart LR
    subgraph Ask[上位机问我 3字节]
        A1[0x10 0xFF 0x7A 进站]
        A2[0x10 0xFF 0x75 下放]
        A3[0x10 0xFF 0x9A 进站+校验]
        A4[0x10 0xFF 0x95 下放+校验]
        A5[0x10 0xFF 0x9D 连续发送]
        A6[0x10 0xFF 0x90 查询版本]
        A7[0x10 0xA0 0x05 系统复位]
    end

    subgraph Answer[我回答]
        B1[有卡-模式1/3: 0x10 0xFF 0x05 +5字节卡号 =8字节]
        B2[有卡-模式2: 0x10 0xFF 0x05 +5字节卡号+BCC =9字节]
        B3[无卡-模式1/3: 0x10 0xFF 0x04 Fail =7字节]
        B4[无卡-模式2: 0x10 0xFF 0x04 Fail+BCC =8字节]
        B5[版本: 0x10 0xFF 0x01 Ver0310 =10字节]
    end

    Ask --> Answer

    style Ask fill:#e3f2fd,stroke:#4A90D9
    style Answer fill:#e8f5e9,stroke:#50B86C
```

### 4.2 命令速查

| 上位机发 | 我做什么 | 我切到什么模式 |
|----------|---------|--------------|
| `0x10 0xFF 0x7A` | 进站方向读卡 | -> 模式1，方向=进站 |
| `0x10 0xFF 0x75` | 下放方向读卡 | -> 模式1，方向=下放 |
| `0x10 0xFF 0x9A` | 进站方向读卡+校验 | -> 模式2，方向=进站 |
| `0x10 0xFF 0x95` | 下放方向读卡+校验 | -> 模式2，方向=下放 |
| `0x10 0xFF 0x9D` | 连续发送 | -> 模式3 |
| `0x10 0xFF 0x90` | 告诉我版本号 | 不变 |
| `0x10 0xA0 0x05` | 软件复位 | 重启 |

### 4.3 什么时候我不理上位机

- 我正在发送数据中（TX busy）
- 我刚刚上报了卡号，还在 100ms 等待期内（准备二次上报）

---

## 五、我的 LED 行为

```mermaid
flowchart TD
    subgraph M12[模式1和模式2]
        G12[绿灯 = 有卡指示<br/>有卡亮, 无卡灭<br/>200ms无新卡自动灭]
        R12[红灯 = 与绿灯互斥<br/>绿灯亮 -> 红灯灭<br/>绿灯灭 -> 红灯亮]
    end

    subgraph M3[模式3]
        G3[绿灯: 有卡亮, 无卡灭]
        R3[红灯: 始终常亮]
    end

    subgraph Switch[模式切换时 绿灯闪烁]
        S1[切到模式1 -> 闪2次]
        S2[切到模式2 -> 闪4次]
        S3[切到模式3 -> 闪6次]
    end

    subgraph Special[特殊:立杆小车黑卡]
        Sp[卡号567位=0x00,0x00,0x5A<br/>两灯全灭 -> 300ms后 -> 两灯同时亮]
    end

    style G12 fill:#50B86C,color:#fff
    style G3 fill:#50B86C,color:#fff
    style R3 fill:#D94A4A,color:#fff
    style Sp fill:#8E44AD,color:#fff
```

**闪烁速度：** 每 ~200ms 翻转一次（一亮一灭算半个周期）

---

## 六、我的系统定时

我内部有一个 **50ms 周期**的节拍，所有定时都是它的倍数：

| 倍数 | 时长 | 用在哪 |
|------|------|--------|
| x1 | 50ms | EM4095 初始化稳定 |
| x2 | 100ms | 卡号二次上报 / 连续模式有卡间隔 |
| x4 | 200ms | 读卡LED自动灭 / 模式切换闪烁半周期 |
| x6 | 300ms | 立杆小车LED延迟 |
| x10 | 500ms | 连续模式无卡上报间隔 |
| x2+ | ~106ms | 看门狗超时 |

---

## 七、我能远程升级

控制器可以通过 UART 给我升级固件，用的是另一套帧格式（不是平时的 `0x10 0xFF` 协议）：

```mermaid
flowchart TD
    Idle[空闲:正常运行] -->|0x81 启动升级| Recv[接收中]
    Recv -->|0x82 数据块 包序号连续递增| Recv
    Recv -->|全部收完| Update[更新中:写Flash]
    Update --> Wait[等待结果]
    Wait -->|0x83 查询成功| Done[完成]
    Wait -->|0x83 查询失败| Fail[失败]
    Recv -->|超时或序号跳跃| Fail

    style Idle fill:#4A90D9,color:#fff
    style Recv fill:#E8A838,color:#fff
    style Update fill:#50B86C,color:#fff
    style Done fill:#50B86C,color:#fff
    style Fail fill:#D94A4A,color:#fff
```

**升级帧格式：**

```
0x55 | CMD | Len(2B) | PackNo(2B) | Data(<=256B) | CRC16(2B) | 0xAA
帧头    命令    长度       包序号         数据          校验       帧尾
```

**安全措施：**
- CRC16-USB 校验（多项式 0x8005）
- 固件用密钥 `"603337"` 加密
- 每包最多重发 5 次
- 上电时自动检查上次升级结果并上报

---

## 八、我出错时会怎么处理

| 我遇到什么 | 我怎么处理 |
|-----------|-----------|
| 感应范围内没有卡 | 回答 Fail |
| 标签头部校验不过 | 丢弃，重读 |
| 标签行/列校验不过 | 丢弃，重读 |
| 标签停止位不对 | 丢弃，重读 |
| 读出来的卡号全零 | 不上报，LED灭 |
| 脉宽超出范围 | 复位解码器，重来 |
| 升级帧头不对 | 解析错误码 1 |
| 升级CMD无效 | 解析错误码 2 |
| 升级帧尾不对 | 解析错误码 3 |
| 升级数据太长 | 解析错误码 4 |
| 升级CRC16不匹配 | 解析错误码 5 |
| 升级包序号不连续 | 忽略这一包 |
| 上位机在我TX忙时问我 | 不吭声 |
| 看门狗超时 | 硬件复位 |

---

## 九、我在系统里的位置

```mermaid
flowchart TB
    PC[PC 服务器<br/>全局调度 / 衣架追踪]

    Ctrl[控制器 一块板管2个站口]

    PC -->|UDP/Ethernet| Ctrl

    subgraph SA[A站口]
        direction LR
        MeIn_A[进站的我 入口]
        MeStore_A[下放的我 工位]
        MeOut_A[出站的我 出口]
    end

    subgraph SB[B站口]
        direction LR
        MeIn_B[进站的我]
        MeStore_B[下放的我]
        MeOut_B[出站的我]
    end

    Ctrl -->|UART| MeIn_A
    Ctrl -->|UART| MeStore_A
    Ctrl -->|UART| MeOut_A
    Ctrl -->|UART| MeIn_B
    Ctrl -->|UART| MeStore_B
    Ctrl -->|UART| MeOut_B

    style MeIn_A fill:#50B86C,color:#fff,stroke:#2d6a4f,stroke-width:3px
    style MeStore_A fill:#50B86C,color:#fff,stroke:#2d6a4f,stroke-width:3px
    style MeOut_A fill:#50B86C,color:#fff,stroke:#2d6a4f,stroke-width:3px
    style PC fill:#4A90D9,color:#fff
    style Ctrl fill:#E8A838,color:#fff
```

**有一个关键事实：我们六个读卡器硬件完全一样，唯一的区别是装在哪。**

---

## 十、上位机怎么跟我说话

### 10.1 控制器的轮询节奏

控制器每 **300ms** 向我发一次轮询命令。我根据有没有卡来回答。

如果控制器连续 **300ms** 没收到我回话 -> 它认定丢包。
如果连续 **1000ms+** 没收到 -> 它判定我掉线了。
我恢复应答后 -> 它判定我恢复了。

### 10.2 控制器按我的安装位置发不同命令

```mermaid
flowchart LR
    Ctrl[控制器] -->|0x10 0xFF 0x7A| InReader[装在入口的我<br/>进站读卡器]
    Ctrl -->|0x10 0xFF 0x75| StoreReader[装在工作站的我<br/>下放读卡器]
    Ctrl -->|0x10 0xFF 0x75| OutReader[装在出口的我<br/>出站读卡器]

    InReader -.-> InAction[读到卡 -> 匹配任务表<br/>匹配:开进站阀<br/>不匹配:放过]
    StoreReader -.-> StoreAction[读到衣架 -> 请求PC授权下放<br/>读到员工卡 -> 登录或退出]
    OutReader -.-> OutAction[读到卡 -> 确认出站<br/>上报PC:衣架已离开]

    style InReader fill:#4A90D9,color:#fff
    style StoreReader fill:#50B86C,color:#fff
    style OutReader fill:#E8A838,color:#fff
```

---

## 十一、我说出卡号之后会发生什么

这是整个系统最核心的故事线——我读出衣架卡号，然后呢？

```mermaid
flowchart TD
    Start([开始]) --> Phase1

    subgraph Phase1[阶段1: 任务下发]
        PC1[PC服务器] -->|下发任务清单| Ctrl1[控制器]
        Ctrl1 --> Ctrl1a[存为 InTaskList]
    end

    Phase1 --> Phase2

    subgraph Phase2[阶段2: 进站识别 - 我在入口]
        MeIn[进站的我 读到卡] --> Ctrl2[控制器]
        Ctrl2 --> Match{卡号在InTaskList里?}
        Match -->|是| OpenValve[开进站阀 -> 衣架进站]
        Match -->|否| Skip[不开阀 -> 衣架继续前进]
        OpenValve --> Rpt1[上报PC: 186 衣架已进站]
        Skip --> Rpt2[上报PC: 187 不在任务清单]
    end

    Phase2 --> Phase3

    subgraph Phase3[阶段3: 下放请求 - 我在工位]
        MeStore[下放的我 读到卡] --> Ctrl3[控制器]
        Ctrl3 -->|152 请求下放授权| PC3[PC服务器]
        PC3 -->|授权| Lift[提升臂下降]
        Lift --> Work[工人操作]
        Work --> LiftUp[操作完成,提升回轨道]
    end

    Phase3 --> Phase4

    subgraph Phase4[阶段4: 出站确认 - 我在出口]
        LiftUp --> OutValve[开出站阀]
        OutValve --> MeOut[出站的我 读到卡]
        MeOut --> Ctrl4[控制器]
        Ctrl4 -->|139 衣架已出站| PC4[PC服务器]
        PC4 --> Rail[更新位置,调度下一站]
    end

    Phase4 --> End([衣架去下一站])

    style MeIn fill:#4A90D9,color:#fff
    style MeStore fill:#50B86C,color:#fff
    style MeOut fill:#E8A838,color:#fff
    style Match fill:#D94A4A,color:#fff
```

---

## 十二、不同站台模式下我的表现

```mermaid
flowchart TD
    subgraph Std[标准站模式 - 普通工作站]
        Std_In[进站的我: 匹配任务表 -> 决定放行]
        Std_Store[下放的我: 读衣架->请求PC授权下放 / 读员工卡->登录退出]
        Std_Out[出站的我: 可能不装或不启用]
    end

    subgraph Bridge[桥接模式 - 中转或过渡站]
        Br_In[进站的我: 同标准站]
        Br_Store[下放的我: 读衣架->不下放,仅请求过站]
        Br_Out[出站的我: 确认衣架离开->上报]
    end

    Br_In -.->|如果坏了| Fallback[跳过ID验证<br/>所有衣架直接放行<br/>安全兜底策略]

    style Std fill:#e8f5e9,stroke:#50B86C
    style Bridge fill:#e3f2fd,stroke:#4A90D9
    style Fallback fill:#ffebee,stroke:#D94A4A
```

---

## 十三、我的状态管理（控制器视角）

```mermaid
flowchart TD
    Start([上电]) --> Normal[NORMAL 正常工作]
    Normal --> Lost[LOST 掉线]
    Lost --> Normal

    NormalDesc[控制器每300ms轮询我<br/>我正常应答] -.-> Normal
    LostDesc[控制器向PC+面板<br/>报告读卡器掉线<br/>继续轮询等待我恢复] -.-> Lost

    LostCfg[连续1000ms+无应答 -> 判定掉线] -.->|触发条件| Normal
    RecvCfg[我恢复应答 -> 判定恢复] -.->|触发条件| Lost

    style Normal fill:#50B86C,color:#fff
    style Lost fill:#D94A4A,color:#fff
```

**每个我都有3个配置项（存在Flash里，可动态修改）：**

| 配置项 | 说明 |
|--------|------|
| 绑定端口 | 我接在哪个UART上 |
| 是否监控 | 控制器要不要盯我的在线状态 |
| 掉线阈值 | 多久不答算掉线（1000ms+） |

---

## 十四、我跟老系统(S2)和新系统(S3)打交道时有什么不同

```mermaid
flowchart TD
    subgraph S2[老系统 S2]
        S2_MCU[上位机: PIC18F97J60 ~41MHz]
        S2_UART[UART: 2路原生 + WK2114 SPI扩4路]
        S2_Cmd[命令: 0x7A / 0x75 / 0x3B 三种]
        S2_Alert[掉线通知: 标志位 + LCD屏显]
        S2_Admin[管理员卡: 3组硬编码在控制器里]
        S2_Update[升级: TCP下载 -> IAP写自身Flash]
        S2_Extra[独有: 高速/低速出站模式可选]
    end

    subgraph S3[新系统 S3]
        S3_MCU[上位机: GD32F407 240MHz]
        S3_UART[UART: 8路原生,独立DMA]
        S3_Cmd[命令: 0x7A / 0x75 统一两种]
        S3_Alert[掉线通知: 标志位+UDP报PC+面板消息队列]
        S3_Admin[管理员卡: PC统一管理,控制器不管]
        S3_Update[升级: UDP下载 -> 存W25QXX -> UART分发给所有我]
        S3_Extra[独有: UART冲突检测, 面板消息队列30+种]
    end

    style S2 fill:#fff3e0,stroke:#E8A838
    style S3 fill:#e3f2fd,stroke:#4A90D9
```

---

## 十五、重构时需要实现什么

### A. 我的核心能力 (Reader 端)

| # | 必须实现 |
|---|---------|
| 1 | 125kHz 载波产生 + EM4100/EM4102 曼彻斯特解码 |
| 2 | 五道校验(头/行/列/停止位/全零) |
| 3 | 3字节轮询协议: `0x10 0xFF 0xXX` -> 应答卡号或Fail |
| 4 | 7条指令: 0x7A/0x75/0x9A/0x95/0x9D/0x90/复位 |
| 5 | 3种模式: 默认/校验/连续，模式切时绿灯闪对应次数 |
| 6 | 双次上报(100ms重复防丢) + TX忙时不响应 |
| 7 | 绿灯(有卡) + 红灯(状态/连续) 全行为 |
| 8 | 50ms 系统节拍基准 |
| 9 | 看门狗 (~106ms) |
| 10 | 远程升级 (0x55帧, CRC16-USB, 密钥"603337") |

### B. 上位机要对我做的事 (Controller 端)

| # | 必须实现 |
|---|---------|
| 1 | 每300ms 向每个我发轮询 |
| 2 | 解析我的应答: 7/8/9字节，提取4字节卡号(大端) |
| 3 | 进站的我: 卡号匹配InTaskList -> 开阀 / 不匹配 -> 放过 |
| 4 | 下放的我: 卡号 -> 请求PC授权 -> 授权后下放机构 |
| 5 | 出站的我: 卡号 -> 确认出站 -> 上报PC |
| 6 | 下放的我兼读员工卡: -> 登录/退出/求助 |
| 7 | 我掉线(1000ms+无应答) -> 上报PC故障 |
| 8 | 我接哪个UART可配置，存在Flash里 |
| 9 | 给我升级: 上位机收固件 -> 通过UART -> 分发给所有我 |

---

## 十六、测试验收清单

### 我的基本功
- [ ] 有卡靠近 -> 绿灯亮，无卡 -> 绿灯灭
- [ ] 卡号上报格式: `0x10 0xFF 0x05` + 4字节卡号(大端)
- [ ] 同一张卡不重复上报（模式3除外）
- [ ] 换卡 -> 立即上报新卡号
- [ ] 非法卡（校验失败/全零）-> 不上报

### 协议交互
- [ ] 0x7A / 0x75 问我 -> 正常答
- [ ] 0x9A / 0x95 问我 -> 答带BCC(末尾=XOR)
- [ ] 0x9D -> 连续模式，红灯常亮
- [ ] 0x90 -> 返回 Ver0310
- [ ] 没卡 -> 答 Fail
- [ ] 我TX忙时不理你
- [ ] 100ms内读到同一张卡 -> 第二次上报

### 模式切换
- [ ] 切模式1 -> 绿灯闪2次
- [ ] 切模式2 -> 绿灯闪4次，数据带BCC
- [ ] 切模式3 -> 绿灯闪6次，红灯常亮

### 掉线恢复
- [ ] 拔掉我的串口 -> 上位机判定我掉线 -> 报PC
- [ ] 插回去 -> 上位机判定我恢复 -> 清故障

### 给我升级
- [ ] 0x81 启动 -> 进入升级态
- [ ] 0x82 顺序发包 -> 正常收
- [ ] 0x82 序号跳跃 -> 忽略
- [ ] 0x83 查询 -> 返回成功/失败
- [ ] CRC16正确
- [ ] 上电自动上报上次升级结果

### 系统联调
- [ ] 进站的我 -> 匹配任务表 -> 开阀放行
- [ ] 进站的我 -> 不匹配 -> 衣架放走
- [ ] 下放的我 -> 读衣架 -> 请求PC -> 授权后下放
- [ ] 下放的我 -> 读员工卡 -> 登录/退出
- [ ] 出站的我 -> 确认出站 -> 上报PC
- [ ] 桥接模式 + 进站的我坏了 -> 直接放行

### 鲁棒性
- [ ] 看门狗超时 -> 我复位
- [ ] 格式错误的轮询 -> 我不崩，继续干活
- [ ] 干扰/非法卡片 -> 我正确拒绝
- [ ] 连续快速刷卡 -> 不丢不重
- [ ] 多个我同时工作 -> 互不干扰
