---
领域:
  - 工作
类型:
  - 方案
项目:
  - "[[SR3读卡器]]"
任务:
  - "[[260802SR3读卡器架构]]"
相关任务:
  - "[[260802SR3读卡器需求]]"
  - "[[260802SR2读卡器行为]]"
状态: 进行
创建时间: 2026-08-02T00:24:42
截止时间: 2026-08-02T23:24:00
完成时间:
---

## 待办列表
- [ ] 整体架构
- [ ] 中断与冲突
- [ ] 业务流程闭环

---

## 1. 架构总图

```mermaid
flowchart TD
    subgraph APP["Application · 主循环"]
        DECIDE["app_mode 决策"]
        STATE["app_card + app_cmd 状态"]
        ACT["app_report + app_led 执行"]
    end

    subgraph MID["Middleware · 主循环"]
        DEC["em4095_core · 解码器"]
        PROTO["proto_10 · 0x10帧协议"]
    end

    subgraph BSP["BSP 驱动层"]
        FIFO["SPSC FIFO ×3 · 无锁缓冲"]
        ISR["ISR · 捕获 / UART RX / UART TX / SysTick"]
        API["bsp_em4095 · bsp_uart · bsp_systick"]
    end

    subgraph HAL["HAL 硬件层"]
        PERIPH["ATIM3 · LPUART1 · SysTick · GPIO"]
    end

    subgraph EXT["External 外部"]
        TAG["EM4100 Tag"]
        EM4095["EM4095 RF前端"]
        HOST["Host 上位机"]
    end

    TAG -.->|"125kHz RF"| EM4095
    EM4095 -->|"DEMOD_OUT"| PERIPH
    PERIPH -->|"中断"| ISR
    ISR -->|"写"| FIFO
    FIFO -->|"主循环消费"| DEC
    DEC -->|"on_id()"| STATE
    STATE -->|"event + cmd"| DECIDE
    DECIDE -->|"report + intent"| ACT
    ACT -->|"写"| FIFO
    FIFO -->|"TXE中断消费"| ISR
    ISR -->|"写DR"| PERIPH
    PERIPH -->|"UART"| HOST

    HOST -->|"UART"| PERIPH

    style APP fill:#1a1a2e,stroke:#FF9800,stroke-width:3px
    style MID fill:#16213e,stroke:#2196F3
    style BSP fill:#0f3460,stroke:#50B86C
    style HAL fill:#1e2632,stroke:#999
```

---

## 2. HAL 硬件层

```mermaid
flowchart TD
    subgraph HAL["HAL 硬件层"]
        ATIM3["ATIM3 Advanced Timer<br/>─────────<br/>模式: 双边沿捕获<br/>数字滤波: f_DTS÷32<br/>16-bit 计数器 @ 1MHz<br/>溢出中断 + 捕获中断"]
        LPUART["LPUART1<br/>─────────<br/>PD05(TX) · PD06(RX)<br/>19200-8-N-1<br/>时钟: PCLK 48MHz<br/>中断: RC / TXE / PE / FE"]
        SYSTICK["SysTick<br/>─────────<br/>1ms 中断<br/>24-bit 递减计数器<br/>时钟: HCLK 48MHz"]
        GPIO["GPIO<br/>─────────<br/>SHD: PD04 推挽<br/>LED_R: PD01 推挽<br/>LED_G: PD02 推挽<br/>DEMOD_IN: PD03 输入"]
    end

    ATIM3 -->|"捕获中断"| NVIC["NVIC<br/>IrqLevel2"]
    LPUART -->|"UART中断"| NVIC
    SYSTICK -->|"SysTick中断"| NVIC["NVIC<br/>IrqLevel3"]
```

---

## 3. BSP 驱动层

### 3.1 BSP 总架构

```mermaid
flowchart TD
    subgraph FIFO["SPSC FIFO 无锁缓冲"]
        direction LR
        F3["edge_fifo<br/>128条 × 3B<br/>{level, interval}"]
        F1["g_rxFifo<br/>256B"]
        F2["g_txFifo<br/>256B"]
    end

    subgraph ISR["ISR 上下文"]
        direction LR
        ISR_EM["ATim3_IRQHandler<br/>─────────<br/>① 读 CCR 锁存值<br/>② interval = cap - last<br/>③ 读捕获方向<br/>④ push edge_fifo<br/><br/>执行: < 2µs<br/>频率: ~3.9kHz"]
        ISR_RX["LpUart1_IRQHandler · RX<br/>─────────<br/>① 读 DR<br/>② push g_rxFifo<br/><br/>执行: < 1µs"]
        ISR_TX["LpUart1_IRQHandler · TX<br/>─────────<br/>① pop g_txFifo<br/>② 写 DR<br/><br/>执行: < 1µs"]
    end

    subgraph API["BSP API · 主循环侧"]
        BSP_EM["bsp_em4095<br/>─────────<br/>start(): SHD高50ms→低50ms<br/>→ 清空edge_fifo→使能ATIM3<br/>stop(): 禁ATIM3中断"]
        BSP_UART["bsp_uart<br/>─────────<br/>read(): spsc_fifo_read_bulk<br/>write(): spsc_fifo_write_bulk<br/>非阻塞，推入txFifo即返回"]
        BSP_TICK["bsp_systick<br/>─────────<br/>1ms 自增计数器<br/>Bsp_SystickElapsed()"]
    end

    ISR_EM -->|"write"| F3
    ISR_RX -->|"write"| F1
    ISR_TX -->|"read"| F2

    F3 -->|"主循环 read"| API
    F1 -->|"主循环 read"| API
    API -->|"主循环 write"| F2

    FIFO -.->|"跨上下文分界线：ISR ↔ 主循环<br/>零锁 · DMB屏障 · 满则丢弃"| ISR
```

### 3.2 SPSC FIFO 读写关系

```
          生产者 (唯一)              消费者 (唯一)
    ┌──────────────────┐    ┌──────────────────┐
    │ 捕获 ISR          │    │ em4095_poll()    │
    │ head 由 ISR 独占  │    │ tail 由主循环独占  │
    │ 只 write          │    │ 只 read           │
    └──────┬───────────┘    └──────┬───────────┘
           │                       │
           ▼                       ▼
    ┌─────────────────────────────────────────┐
    │              edge_fifo                   │
    │  容量 = 128, 可缓冲 32ms (一整帧)         │
    │  write(head++) · DMB · read(tail++)     │
    │  满则静默丢弃 → ISR 不阻塞               │
    └─────────────────────────────────────────┘

    g_rxFifo: ISR_RX(write) → 主循环(read)
    g_txFifo: 主循环(write) → ISR_TX(read)
```

---

## 4. Middleware 中间件层

### 4.1 em4095_core 解码器

```mermaid
flowchart TD
    subgraph DEC["em4095_core 解码器"]
        direction TB

        INPUT["输入: edge_fifo<br/>{level, interval}"]

        CLASS["classify_interval()<br/>─────────<br/>DEBRIS: < 0.75×base<br/>SHORT: 0.75~1.375×base<br/>AMBIGUOUS: 1.375~1.625×base<br/>LONG: 1.625~2.50×base<br/>TIMEOUT: > 2.50×base"]

        FSM["process_edge() 状态机<br/>─────────<br/>SEEK_PREAMBLE: 找≥14交替SHORT<br/>RECEIVE: 逐位解64bits<br/>VERIFY: 5关校验"]

        VOTE["vote_and_output()<br/>─────────<br/>滑动窗口3帧<br/>2/3一致 → on_id()"]
    end

    subgraph OUT["输出"]
        CB["on_id(id[5], checksum_ok)"]
    end

    INPUT --> CLASS
    CLASS --> FSM
    FSM --> VOTE
    VOTE --> CB

    style DEC fill:#16213e,stroke:#2196F3
```

### 4.2 解码状态机

```mermaid
stateDiagram-v2
    [*] --> SEEK_PREAMBLE

    state SEEK_PREAMBLE {
        [*] --> 扫描边沿
        扫描边沿 --> 分类间隔 : 收到edge
        分类间隔 --> 检查交替 : SHORT
        分类间隔 --> 重置 : not SHORT
        检查交替 --> 累加 : 方向交替
        检查交替 --> 重置 : 同方向
        累加 --> 锁定 : cnt ≥ 14
    }

    state RECEIVE {
        逐位解码 --> SHORT分支 : SHORT
        逐位解码 --> LONG分支 : LONG
        逐位解码 --> 碎片跳过 : DEBRIS
        逐位解码 --> 超时重置 : TIMEOUT
        SHORT分支 --> 得1bit : need_skip=0
        SHORT分支 --> 丢弃 : need_skip=1
        LONG分支 --> 复制上一位
    }

    得1bit --> 检查收满 : bit_idx++
    丢弃 --> 检查收满
    复制上一位 --> 检查收满
    检查收满 --> VERIFY : bit_idx≥64
    检查收满 --> 逐位解码

    SEEK_PREAMBLE --> RECEIVE : 锁定·avg_short
    RECEIVE --> SEEK_PREAMBLE : TIMEOUT/err>3

    state VERIFY {
        [*] --> 停止位 : bit[63]==0?
        停止位 --> 行校验 : 10行偶校验
        停止位 --> 失败
        行校验 --> 列校验 : 10行全过
        行校验 --> 失败
        列校验 --> 非全零 : 4列全过
        列校验 --> 失败
        非全零 --> 提取ID : id≠0x0000000000
        非全零 --> 失败
    }

    VERIFY --> 帧投票 : 5关通过
    VERIFY --> SEEK_PREAMBLE : 失败
    帧投票 --> 输出卡号 : 2/3一致
    帧投票 --> SEEK_PREAMBLE
    输出卡号 --> SEEK_PREAMBLE
```

### 4.3 proto_10 协议层

```c
// 纯工具函数，无状态
bool   proto10_verify_frame(uint8_t *buf, uint8_t len);  // 0x10帧头校验
uint8_t proto10_calc_bcc(uint8_t *buf, uint8_t len);     // XOR BCC
void   proto10_build_card(uint8_t *out, uint8_t *id, bool bcc);   // 拼卡号包
void   proto10_build_fail(uint8_t *out, bool bcc);                // 拼Fail包
```

---

## 5. Application 应用层

### 5.1 应用层总架构

```mermaid
flowchart TD
    subgraph SOURCE["数据来源"]
        DEC_CB["em4095_core<br/>on_id(id, checksum)"]
        RX["g_rxFifo · UART字节流"]
        TICK["bsp_systick · 1ms"]
    end

    subgraph CARD["app_card · 卡状态管理"]
        direction TB
        C1["卡属性<br/>id[5] · present · is_black · present_ms"]
        C2["卡事件<br/>ARRIVED / DEPARTED"]
        C3["事件缓冲 深2"]
        C4["超时检测<br/>lost_timer > 200ms → DEPARTED"]
    end

    subgraph CMD["app_cmd · 指令解析"]
        direction TB
        D1["帧拼接状态机<br/>找0x10→读长度→收数据→BCC"]
        D2["7种指令识别<br/>0x7A/75/9A/95/9D/90/A0_05"]
    end

    subgraph MODE["app_mode · 唯一决策者"]
        direction TB
        M1["模式 · 方向<br/>mode 1/2/3 · manner 0x55/0xAA"]
        M2["定时器<br/>进站二次上报 · 冷却 · 模式3定时"]
        M3["决策逻辑<br/>综合 event + cmd + mode → 行为"]
        M4["忙保护<br/>cooling 时静默忽略指令"]
    end

    subgraph EXEC["执行器"]
        RPT["app_report<br/>拼帧 · BCC · 写txFifo"]
        LED["app_led<br/>意图→GPIO · 黑卡时序"]
    end

    DEC_CB -->|"on_id"| CARD
    RX -->|"字节"| CMD
    TICK --> CARD
    TICK --> MODE
    TICK --> LED

    CARD -->|"consume_event()"| MODE
    CARD -->|"查询: present? id? black?"| MODE
    CMD -->|"consume()"| MODE

    MODE -->|"report_request"| RPT
    MODE -->|"led_intent"| LED

    style MODE fill:#1a1a2e,stroke:#FF9800,stroke-width:3px
```

### 5.2 app_card 内部

```mermaid
flowchart TD
    ON_ID["on_id(id, checksum)"] --> CHK{"checksum ok?"}

    CHK -->|"否"| DROP["忽略此帧"]

    CHK -->|"是"| BLACK{"is_black(id)?<br/>Card[5..7]==00,00,5A"}

    BLACK -->|"是"| SET_BLACK["info.is_black = true<br/>info.present = true<br/>memcpy info.id<br/>不产生事件"]

    BLACK -->|"否"| CMP{"memcmp(id, last_id)?"}

    CMP -->|"不同"| NEW["push ARRIVED<br/>更新 info.id + last_id<br/>info.is_black = false<br/>info.present = true<br/>info.present_ms = 0"]

    CMP -->|"相同"| SAME["info.present_ms += 32<br/>无事件"]

    SET_BLACK --> TICK_NEXT["等待下一帧"]
    NEW --> TICK_NEXT
    SAME --> TICK_NEXT
    DROP --> TICK_NEXT

    TICK["tick(elapsed)"] --> LOST{"present &&<br/>lost_timer > 200?"}
    LOST -->|"是"| PUSH_LOST["push DEPARTED<br/>info.present = false<br/>info.is_black = false"]
    LOST -->|"否"| LOST_NEXT["lost_timer += elapsed"]

    PUSH_LOST --> DONE
    LOST_NEXT --> DONE["return"]

    style SET_BLACK fill:#9C27B0,color:#fff
    style NEW fill:#50B86C,color:#fff
    style PUSH_LOST fill:#D94A4A,color:#fff
```

**对外接口：**

```c
// 事件（消费即清）
card_event_t app_card_consume_event(app_card_t *c);

// 属性（随时可查）
bool     app_card_is_present(app_card_t *c);
bool     app_card_is_black(app_card_t *c);
uint8_t* app_card_get_id(app_card_t *c);
uint16_t app_card_present_ms(app_card_t *c);
```

### 5.3 app_cmd 内部

```mermaid
flowchart TD
    RX["g_rxFifo 来字节"] --> S0{"state == 0<br/>找 0x10"}
    S0 -->|"是0x10"| S1["state=1"]
    S0 -->|"否"| S0_DROP["丢弃"]

    S1 -->|"下字节"| CHK_FF{"== 0xFF?"}
    CHK_FF -->|"是"| S2["state=2"]
    CHK_FF -->|"否"| RESET["state=0"]

    S2 -->|"下字节 = len"| S3["state=3 · idx=0"]

    S3 -->|"收字节"| ACCUM["buf[3+idx++] = byte"]
    ACCUM -->|"idx >= len"| DONE["收完"]

    DONE --> BCC_CHK{"has_bcc?"}
    BCC_CHK -->|"是"| VERIFY["verify_bcc()"]
    BCC_CHK -->|"否"| SET
    VERIFY --> SET["cmd.ready = true<br/>cmd.type = parse()"]
    SET --> S0
```

**对外接口：**

```c
typedef enum { CMD_NONE, CMD_7A, CMD_75, CMD_9A, CMD_95, CMD_9D, CMD_90, CMD_A0_05, CMD_INVALID } cmd_type_t;

void      app_cmd_poll(app_cmd_t *c, SPSC_FIFO *rx);
cmd_type_t app_cmd_consume(app_cmd_t *c);
```

### 5.4 app_mode 内部

```mermaid
flowchart TD
    ENTRY["app_mode_update()"] --> EV["ev = consume_event()"]
    ENTRY --> CMD_CHK["cmd.ready?"]

    EV --> EV_SW{"ev?"}
    EV_SW -->|"ARRIVED"| ARRIVED
    EV_SW -->|"DEPARTED"| DEPARTED
    EV_SW -->|"NONE"| TIMERS

    ARRIVED["新卡到场"] --> ARR_MODE{"mode?"}
    ARR_MODE -->|"1/2"| ARR_REPORT["schedule: REPORT_CARD"]
    ARR_MODE -->|"1/2"| ARR_LED["led_intent = CARD_OK"]
    ARR_MODE -->|"1/2"| ARR_MANNER{"manner?"}
    ARR_MANNER -->|"0x55 进站"| ARR_2ND["启动二次上报定时器 100ms"]
    ARR_MANNER -->|"0xAA 下放"| ARR_NONE["无额外动作"]
    ARR_MODE -->|"3"| ARR_M3["不主动上报 · led_intent = MODE3_CARD"]

    DEPARTED["卡离场"] --> DEP_MODE{"mode?"}
    DEP_MODE -->|"1/2"| DEP_LED["led_intent = NO_CARD"]
    DEP_MODE -->|"3"| DEP_M3["led_intent = MODE3_NOCARD"]

    TIMERS["定时器处理"] --> T2{"second_send_timer?"}
    T2 -->|"到期"| T2_SEND["二次上报 REPORT_CARD<br/>cooldown_timer = 50ms<br/>cooling = true"]
    T2 -->|"未到期"| T3{"cooling?"}
    T3 -->|"到期"| T3_END["cooling = false"]
    T3 -->|"未到期"| T4{"mode3?"}
    T4 -->|"100ms到期·有卡"| T4_SEND["report CARD + BCC"]
    T4 -->|"500ms到期·无卡"| T4_FAIL["report FAIL + BCC"]

    CMD_CHK -->|"cmd.ready"| BUSY{"cooling?"}
    BUSY -->|"是"| SKIP["静默忽略"]
    BUSY -->|"否"| BCC{"bcc_ok?"}
    BCC -->|"否"| SKIP
    BCC -->|"是"| DISPATCH

    DISPATCH{"cmd?"}
    DISPATCH -->|"0x7A"| A1["manner=0x55 · reply FAIL"]
    DISPATCH -->|"0x75"| A2["manner=0xAA · present?→CARD:FAIL"]
    DISPATCH -->|"0x9A"| A3["mode=2 · manner=0x55 · reply FAIL+BCC"]
    DISPATCH -->|"0x95"| A4["mode=2 · manner=0xAA · present?→CARD:FAIL+BCC"]
    DISPATCH -->|"0x9D"| A5["mode=3 · 不应答"]
    DISPATCH -->|"0x90"| A6["reply VERSION"]
    DISPATCH -->|"0xA0+0x05"| A7["NVIC_SystemReset()"]
    DISPATCH -->|"INVALID"| SKIP

    SKIP --> DONE
    A1 & A2 & A3 & A4 & A5 & A6 & A7 --> DONE
    T2_SEND & T3_END & T4_SEND & T4_FAIL --> DONE
    ARR_REPORT --> DONE
    ARR_NONE --> DONE
```

**核心规则：**
- cooling 期间忽略所有指令 —— 卡号上报优先级最高
- 模式3 不响应大多数指令（0x7A/0x75/0x9A/0x95 可退出）
- `app_mode` 不拼帧、不写 GPIO，只设 `report_request` + `led_intent`

### 5.5 app_report 内部

```mermaid
flowchart LR
    REQ["report_request<br/>type + id + bcc_flag"] --> BUILD["拼帧"]
    BUILD --> PUSH["逐字节写入 g_txFifo<br/>(非阻塞, 不等待)"]
    PUSH --> TX["TXE 中断异步发送"]
```

**对外接口：**

```c
typedef enum { REPORT_CARD, REPORT_FAIL, REPORT_VERSION } report_type_t;
void app_report_send(report_type_t type, uint8_t *card_id, bool with_bcc);
```

### 5.6 app_led 内部

```mermaid
flowchart TD
    INTENT["set_intent(intent)"] --> CMP{"与当前不同?"}
    CMP -->|"是"| RESET["重置 state + timer"]
    CMP -->|"否"| TICK

    RESET --> APPLY["立即应用 GPIO"]

    TICK["tick(elapsed)"] --> BLACK{"intent == BLACK_CARD?"}
    BLACK -->|"是"| BC_STATE{"state?"}
    BC_STATE -->|"0: 全灭"| BC_WAIT{"timer >= 300?"}
    BC_WAIT -->|"是"| BC_ON["全亮 · state=1"]
    BC_WAIT -->|"否"| BC_CONT["timer += elapsed"]
    BC_STATE -->|"1: 全亮"| BC_DONE["保持"]

    BLACK -->|"否"| DONE["无时序, 保持当前 GPIO"]

    style BLACK fill:#9C27B0,color:#fff
```

**意图表：**

| intent | 红灯 | 绿灯 | 时序 |
|--------|------|------|------|
| INIT | 亮 | 灭 | 瞬时 |
| CARD_OK | 灭 | 亮 | 瞬时 |
| NO_CARD | 亮 | 灭 | 瞬时 |
| BLACK_CARD | 灭→亮 | 灭→亮 | 300ms |
| MODE3_CARD | 亮 | 亮 | 瞬时 |
| MODE3_NOCARD | 亮 | 灭 | 瞬时 |

---

## 6. 主循环

```c
void main(void) {
    Bsp_Init();
    app_card_init(&g_card);
    app_mode_init(&g_mode);
    app_led_set_intent(LED_INTENT_INIT);

    while (1) {
        uint16_t elapsed = Bsp_SystickElapsed();

        em4095_poll(&g_dec);                       // ① 解码 → on_id → app_card
        app_cmd_poll(&g_cmd, &g_rxFifo);           // ② 指令拼帧
        app_card_tick(&g_card, elapsed);            // ③ 卡超时检测
        app_mode_update(&g_mode, &g_card, &g_cmd, elapsed);  // ④ 唯一决策
        app_report_flush(&g_mode, &g_txFifo);       // ⑤ 执行上报
        app_led_tick(&g_led, elapsed);              // ⑥ 执行 LED
    }
}
```

---

## 7. 中断与冲突

### 7.1 ISR 原则

```
ISR = 只记录，不判断
─────────────────────
读寄存器 → 算差值 → 写 FIFO
不做: 毛刺判定 · 分类 · 解码 · 校验 · 投票
```

### 7.2 优先级

```
IrqLevel2  ATIM3 捕获 (~3.9kHz, <2µs)
           LPUART1  (<1µs)
           → 同优先级，NVIC 按中断号顺序执行，不抢占
           → ATIM3 CCR 硬件锁存，ISR 延迟也不丢值

IrqLevel3  SysTick 1ms
```

### 7.3 防误码

| 场景 | 防护 |
|------|------|
| 毛刺假边沿 | ATIM3 硬件数字滤波 f_DTS÷32 |
| ISR 被 UART 抢占 | CCR 硬件锁存，ISR 延迟不丢值 |
| 主循环解码到一半 ISR 写入 | SPSC: 写方只动 head, 读方只动 tail |
| 解码耗时阻塞 | classify + process_edge 每条 <5µs，FIFO 128条缓冲32ms |
| TX 发送阻塞主循环 | Bsp_UartWrite() 非阻塞，推入 txFifo 即返回 |
| 状态机死锁 | 所有状态都有 TIMEOUT 出口 |
| 卡离开后残留 | 200ms 超时 → DEPARTED → 所有状态清零 |

### 7.4 时序

| 参数 | 值 |
|------|-----|
| 边沿间隔 | 256µs / 512µs |
| ISR | < 2µs @48MHz |
| edge_fifo = 128条 | 缓冲 32ms (一整帧) |
| 溢出条件 | 主循环停滞 > 32ms (不会发生) |
| 解码延迟 | ~66ms (2帧投票) |

---

## 8. 业务流程闭环

### 8.1 进站 (Send_Manner = 0x55)

```mermaid
sequenceDiagram
    participant TAG as Tag
    participant DEC as em4095_core
    participant CARD as app_card
    participant MODE as app_mode
    participant RPT as app_report
    participant LED as app_led
    participant HOST as Host

    TAG->>DEC: Manchester 帧
    DEC->>CARD: on_id(0xAABBCCDDEE, ok)

    CARD->>CARD: 与 last_id 不同 · 非黑卡<br/>push ARRIVED

    MODE->>CARD: consume_event() → ARRIVED
    MODE->>MODE: manner=0x55 进站<br/>→ 立即上报 + 100ms二次
    MODE->>RPT: send(CARD, id)
    MODE->>LED: set_intent(CARD_OK)
    RPT->>HOST: 0x10 0xFF 0x05 AA BB CC DD EE

    Note over MODE: 100ms 后 → 二次上报
    MODE->>RPT: send(CARD, id)
    RPT->>HOST: 0x10 0xFF 0x05 AA BB CC DD EE
    Note over MODE: 50ms 冷却 (忽略指令)

    Note over MODE: 冷却结束
    HOST->>MODE: (g_rxFifo) → CMD_7A
    MODE->>RPT: send(FAIL)
    RPT->>HOST: 0x10 0xFF 0x04 "Fail"

    Note over TAG,HOST: 卡离开...
    CARD->>CARD: tick: lost_timer > 200ms<br/>push DEPARTED
    MODE->>CARD: consume_event() → DEPARTED
    MODE->>LED: set_intent(NO_CARD)
```

### 8.2 下放 (Send_Manner = 0xAA)

```mermaid
sequenceDiagram
    participant TAG as Tag
    participant DEC as em4095_core
    participant CARD as app_card
    participant MODE as app_mode
    participant RPT as app_report
    participant HOST as Host

    TAG->>DEC: Manchester 帧
    DEC->>CARD: on_id(id, ok)
    CARD->>CARD: push ARRIVED

    MODE->>CARD: consume_event() → ARRIVED
    MODE->>MODE: manner=0xAA → 仅一次
    MODE->>RPT: send(CARD, id)
    RPT->>HOST: 0x10 0xFF 0x05 ...

    Note over MODE: 无二次 · 无冷却

    HOST->>MODE: CMD_75
    MODE->>CARD: is_present()? → true
    MODE->>RPT: send(CARD, id)
    RPT->>HOST: 0x10 0xFF 0x05 ...

    Note over TAG,HOST: 卡离开...
    HOST->>MODE: CMD_75
    MODE->>CARD: is_present()? → false
    MODE->>RPT: send(FAIL)
    RPT->>HOST: 0x10 0xFF 0x04 "Fail"
```

### 8.3 数据包格式

```
卡号包 (模式1):      0x10 0xFF 0x05 [ID0..4]           → 8B
卡号包 (模式2/3):    0x10 0xFF 0x05 [ID0..4] [BCC]     → 9B
Fail包 (模式1):      0x10 0xFF 0x04 'F' 'a' 'i' 'l'    → 7B
Fail包 (模式2/3):    0x10 0xFF 0x04 'F' 'a' 'i' 'l' [BCC] → 8B
版本包:             0x10 0xFF 0x07 'V' 'e' 'r' '0' '3' '1' '0' → 10B

BCC = 前N字节 XOR
```

---

## 9. 源文件组织

```
01_Hardware/bsp/
├── inc/
│   ├── spsc_fifo.h              # SPSC FIFO (已有)
│   ├── bsp_em4095.h            # EM4095 抽象 + edge_fifo
│   ├── bsp_uart.h / bsp_systick.h / bsp_clk.h / bsp_config.h  (已有)
└── src/
    ├── bsp_em4095.c / bsp_uart.c / bsp_systick.c / bsp_clk.c / bsp.c

01_Hardware/driver/              # DDL (按需: atim3/lpuart/gpio/sysctrl/...)
01_Hardware/f002/common/         # MCU 平台 (已有)

02_Midware/
├── em4095/
│   ├── em4095_core.h            # edge_t · decoder_t · class_t · state_t
│   └── em4095_core.c            # classify + process_edge + verify + vote
└── protocol/
    ├── proto_10.h / proto_10.c  # 0x10 帧工具

03_Application/
├── main.c                       # 6行主循环
├── app_card.h / app_card.c      # 卡状态 + 事件
├── app_cmd.h / app_cmd.c        # 指令解析
├── app_mode.h / app_mode.c      # 唯一决策
├── app_report.h / app_report.c  # 拼帧发送
├── app_led.h / app_led.c        # LED 意图→GPIO

Bootloader/                      # 独立工程
├── boot_main.c / boot_proto_55.c / boot_flash.c / boot_crc.c / boot_crypto.c
```

---

## 10. Flash 布局

```
0x0000 ┌──────────────────────┐
       │     Bootloader        │  ~6KB
0x1C00 ├──────────────────────┤  ← APP_START_ADDR
       │     Application       │  最大 10KB
0x4600 ├──────────────────────┤  ← UPD_PARAM_FLASH_ADDR
       │   Upgrade Param       │  512B
0x4800 └──────────────────────┘
```

---

## 11. 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 采集方案 | Input Capture | ISR ~3.9kHz，CCR 硬件锁存 |
| ISR 策略 | 只记录不判断 | <2µs，无状态 |
| 边沿 FIFO | SPSC × 128 | 零锁，缓冲 32ms |
| **模块耦合** | **主循环串联，状态结构体交换** | 无回调嵌套，无互相调用 |
| **事件 vs 属性** | **ARRIVED/DEPARTED 事件 + present/id 属性** | 事件触发主动行为，属性服务被动查询 |
| **LED 架构** | **意图驱动** | mode 说"绿灯"，led 管时序 |
| 帧投票 | 2/3 一致 | 66ms |
| 校验 | 5 关 | 停止位+10行+4列+非全零+脉宽 |
| 协议 | 0x10 帧 | 向下兼容 SR2 的 19 项行为 |
| 忙保护 | cooling 静默忽略 | 卡号优先级 > 指令 |
