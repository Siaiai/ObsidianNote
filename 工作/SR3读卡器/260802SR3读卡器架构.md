---
领域:
  - 工作
类型:
  - 方案
项目:
  - "[[SR3读卡器]]"
任务:
  - "[[260802SR3读卡器架构]]"
状态: 进行
创建时间: 2026-08-02T00:24:42
截止时间: 2026-08-02T23:24:00
完成时间:
---

## 待办列表
- [ ] 整体架构
- [ ] 中断与冲突
- [ ] 业务流程闭环

---

## 1. 整体架构

```mermaid
flowchart TD
    subgraph APP["Application · 主循环"]
        direction LR
        CMD["指令解析"] --> SM["状态机<br/>IDLE→READING→REPORTING"]
        SM --> FMT["响应格式化"]
    end

    subgraph MID["Middleware · 主循环"]
        DEC["em4095_core<br/>classify → process_edge → verify → vote"]
    end

    subgraph BSP["BSP 驱动层"]
        subgraph FIFO["SPSC FIFO 无锁缓冲"]
            F3["edge_fifo (128条)<br/>{level, interval}"]
            F1["g_rxFifo (256B)<br/>ISR写 → 主循环读"]
            F2["g_txFifo (256B)<br/>主循环写 → ISR读"]
        end
        subgraph ISR["ISR 上下文"]
            ISR_EM["捕获 ISR<br/>读CCR → 算interval"]
            ISR_RX["RX ISR<br/>读 DR → 写 rxFifo"]
            ISR_TX["TX ISR<br/>读 txFifo → 写 DR"]
        end
        BSP_EM["bsp_em4095<br/>ATIM3 启停控制"]
        BSP_UART["bsp_uart<br/>UART 读写接口"]
    end

    subgraph HAL["HAL 硬件层"]
        direction LR
        ATIM3["ATIM3<br/>双边沿捕获<br/>数字滤波"]
        LPUART["LPUART1<br/>PD05/PD06<br/>19200bps 8N1"]
        SYSTICK["SysTick<br/>1ms"]
    end

    subgraph EXT["External 外部"]
        direction LR
        TAG["EM4100 Tag"]
        EM4095["EM4095<br/>125kHz RF前端"]
        HOST["Host 上位机"]
    end

    %% ===== 读卡数据流 =====
    TAG -.->|"125kHz RF"| EM4095
    EM4095 -->|"DEMOD_OUT"| ATIM3
    ATIM3 -->|"捕获中断 ~3.9kHz"| ISR_EM
    ISR_EM -->|"写"| F3
    F3 -->|"主循环消费"| DEC
    DEC -->|"40-bit ID"| SM

    %% ===== 串口 RX 流 =====
    HOST -->|"RX"| LPUART
    LPUART -->|"RC中断"| ISR_RX
    ISR_RX -->|"写"| F1
    F1 -->|"主循环读取"| CMD

    %% ===== 串口 TX 流 =====
    FMT -->|"写"| F2
    F2 -->|"TXE中断消费"| ISR_TX
    ISR_TX -->|"写DR"| LPUART
    LPUART -->|"TX"| HOST

    %% ===== 时基 =====
    SYSTICK -->|"1ms中断"| SM

    %% ===== 控制流 =====
    SM -->|"start/stop"| BSP_EM
    BSP_EM -->|"配置使能"| ATIM3
    SM -->|"read/write"| BSP_UART
    BSP_UART -->|"配置"| LPUART
```

```mermaid
flowchart TD
    subgraph APP["Application · 主循环"]
        direction LR
        CMD["指令处理"] --> SM["EM4095处理"]
        SM --> FMT["led处理"]
    end
```

### 分层职责

| 层           | 上下文  | 做什么                                     | **不做什么**    |
| ----------- | ---- | --------------------------------------- | ----------- |
| ISR         | 中断   | 读 CCR、算 interval、写 FIFO                 | 不解码、不校验、不判断 |
| SPSC FIFO   | 跨上下文 | 无锁数据通道                                  | 不阻塞、不关中断    |
| Middleware  | 主循环  | classify → process_edge → verify → vote | 不碰硬件寄存器     |
| Application | 主循环  | 状态机、指令解析、上报                             | 不碰硬件寄存器     |

---

## 2. 中断与冲突 — 如何从架构上防止误码

### 2.1 核心原则：ISR 只记录，不判断

```mermaid
flowchart TD
    subgraph isr["ISR · 执行时间 &lt;2µs"]
        I1["① 读 CCR 锁存值"] --> I2["② interval = cap - last_cap"]
        I2 --> I3["③ 读捕获方向 (上升/下降)"]
        I3 --> I4["④ 写 {level, interval} 到 edge_fifo"]
    end

    subgraph main["主循环 · 无时间约束"]
        M1["classify_interval()"] --> M2["process_edge() 状态机"]
        M2 --> M3["verify_frame()"]
        M3 --> M4["vote_and_output()"]
    end

    isr -->|"edge_fifo"| main

    note1["ISR 内不做:<br/>· 毛刺判定<br/>· SHORT/LONG 分类<br/>· 同步头搜索<br/>· 校验<br/>· 投票<br/>全部推迟到主循环"]
```

### 2.2 为什么 SPSC FIFO 能防止 ISR 被阻塞

```
         生产者 (唯一)              消费者 (唯一)
    ┌──────────────────┐    ┌──────────────────┐
    │ 捕获 ISR          │    │ em4095_poll()    │
    │                  │    │                  │
    │ head 由 ISR 独占  │    │ tail 由主循环独占  │
    │ 只写不读          │    │ 只读不写          │
    └──────┬───────────┘    └──────┬───────────┘
           │                       │
           ▼                       ▼
    ┌─────────────────────────────────────────┐
    │              edge_fifo                  │
    │  · SPSC 模型：两方各持一个指针            │
    │  · 零互斥锁 → ISR 永远不会等主循环释放锁   │
    │  · DMB 屏障 → 数据对消费者立即可见        │
    │  · 满时静默丢弃 → ISR 不阻塞             │
    └─────────────────────────────────────────┘
```

**如果主循环解码耗时 5ms**（校验 + 投票 + 日志输出），ISR 仍然每 256µs 写入一次——二者完全并行。ISR 不关心主循环在做什么，FIFO 也不上锁。

### 2.3 为什么 ISR 不会被 UART 抢占

```
中断优先级 (0=最高, 3=最低):

  IrqLevel0  [保留]
  IrqLevel1  [保留]
  IrqLevel2  ATIM3 捕获中断    ← 边沿捕获，不能丢
             LPUART1 中断      ← RX/TX，有 FIFO 缓冲
  IrqLevel3  SysTick           ← 1ms 时基，精度要求低
```

ATIM3 和 LPUART1 同优先级时，NVIC 按中断号裁决（顺序执行，不会被抢占）。但即便 LPUART1 先响应、执行 RX/TX ISR（< 1µs），ATIM3 捕获的 CCR 值**已被硬件锁存**——定时器 CCR 寄存器在边沿瞬间就冻结了，ISR 晚几 µs 去读也不会丢值或错值。

### 2.4 为什么不会"解析到一半被打断"

| 潜在问题 | 架构如何避免 |
|----------|------------|
| ISR 执行解码 → 被打断丢状态 | **ISR 不做解码**，只写 FIFO。ISR 是无状态的（static 变量只有 last_cap/last_level） |
| 主循环解码到一半 ISR 写入 | SPSC 模型保证**写方只动 head、读方只动 tail**。主循环读 FIFO 不影响 ISR 写入，反之亦然 |
| 毛刺触发假中断 → 干扰解析 | ATIM3 **硬件数字滤波**（f_DTS ÷ 32）在边沿采集前就滤掉毛刺。逃逸的碎片由 `classify_interval()` → `DEBRIS` 在主循环丢弃 |
| 解码耗时阻塞主循环 | `process_edge()` 每次调用只处理一条边沿，O(1) 操作。主循环 `poll()` 批量消费但随时可中断（整个 while(wr != rd) 循环耗时 < 100µs per 边沿） |
| TX 大包发送阻塞主循环 | `Bsp_UartWrite()` 非阻塞——数据推入 txFifo 即返回。TXE 中断异步逐字节发送，主循环不等待 |
| 卡离开后状态机死锁 | decode 每个状态都有 TIMEOUT 出口。`SEEK_PREAMBLE` 中等不到 SHORT → `short_cnt = 0` 循环；`RECEIVE` 中 `interval > 2.5×base` → 触发 reset 回 `SEEK_PREAMBLE` |

### 2.5 时序分析：最坏情况下会丢边沿吗

```
一个 EM4100 帧 = 128 个边沿，帧周期 ≈ 32.8ms
平均边沿间隔 ≈ 256µs

ISR 执行时间 < 2µs @48MHz
主循环一轮最大耗时估算:
  · em4095_poll(): 消费 N 条边沿，每条 < 5µs (classify + process_edge)
  · 指令解析 + 响应格式化: < 300µs (不含等待)
  · 主循环单轮最坏 ≈ 1ms (N=50 边沿 + UART 处理)

128 条 FIFO 在 1ms 主循环周期内能缓冲:
  256µs/边沿 → 1ms 约产生 4 个边沿
  FIFO 128 条 → 可缓冲 32ms 的边沿数据 = 整整一帧
```

**结论**：即使主循环偶尔慢到 5ms 一轮（比如正在格式化输出），128 条 FIFO 也能吸收 32ms 的数据，不会溢出丢边沿。溢出窗口是主循环停滞超过 32ms——这在实际场景不会发生（没有任何阻塞调用）。

---

## 3. 业务流程闭环

### 3.1 应用层状态机

```mermaid
stateDiagram-v2
    [*] --> INIT

    INIT --> IDLE : Bsp_Init() 完成

    state IDLE {
        [*] --> 等待指令
        等待指令 --> 解析帧 : UART 收到完整行
    }

    IDLE --> READING : AT+SCAN / AUTO=1 超时到期

    state READING {
        [*] --> 使能EM4095 : start()
        使能EM4095 --> 轮询 : em4095_poll() 每轮调用
        轮询 --> 轮询 : 解码中 (SEEK/RECEIVE)
        轮询 --> 拿到卡号 : on_id() 回调触发
        轮询 --> 超时 : N ms 无卡
    }

    READING --> REPORTING : 卡号就绪
    READING --> IDLE : 超时 / AT+CANCEL

    REPORTING --> IDLE : TX FIFO 排空 · 单次模式
    REPORTING --> READING : 自动上报模式

    note right of READING
        poll() 内部:
        · 消费 edge_fifo 所有边沿
        · 解码出错 → 自动重置
        · 校验失败 → 自动回 SEEK
        · 投票通过 → 回调 on_id
    end note
```

### 3.2 完整闭环：从卡靠近到串口输出

```mermaid
sequenceDiagram
    participant TAG as EM4100 Tag
    participant RF as EM4095
    participant TIM as ATIM3 硬件
    participant ISR as 捕获 ISR
    participant FIFO as edge_fifo
    participant DEC as em4095_core
    participant APP as main()
    participant UART as Host

    Note over TAG,UART: ① 卡靠近，开始发送连续帧 (每帧 ~32ms，周期间隔 ~33ms)

    TAG->>RF: 125kHz 负载调制 Manchester
    RF->>TIM: DEMOD_OUT 数字基带 (0/1 方波)

    loop 每个边沿 (~256µs/次)
        TIM->>TIM: 硬件锁存 CCR + 硬件数字滤波去毛刺
        TIM->>ISR: 捕获中断
        ISR->>ISR: interval = CCR - last_CCR
        ISR->>FIFO: push {level, interval}
    end

    Note over TAG,UART: ② 主循环每轮消费 FIFO

    loop 主循环轮询
        APP->>FIFO: while(wr != rd) pop edge
        FIFO->>DEC: process_edge(level, interval)
        DEC->>DEC: SEEK_PREAMBLE: 检测 ≥14 交替 SHORT
        DEC->>DEC: → 锁定! avg_short 已求得
        DEC->>DEC: RECEIVE: 逐位解 64 bits
        DEC->>DEC: VERIFY: 同步头+行校验+列校验+停止位
        DEC->>DEC: 帧投票: 连续2帧 ID 一致
    end

    Note over TAG,UART: ③ 卡号输出

    DEC->>APP: on_id(id[5])
    APP->>APP: 格式化 "+CARD:XXXXXXXXXX\r\n"
    APP->>FIFO: Bsp_UartWrite() → 推入 txFifo
    FIFO->>UART: TX ISR 逐字节发送
    UART-->>APP: Host 收到卡号

    Note over TAG,UART: ④ 循环继续 (卡仍在场)

    DEC->>DEC: → SEEK_PREAMBLE (收下一帧)
```

### 3.3 解码器内闭环（故障自动恢复）

```mermaid
flowchart TD
    START["process_edge() 入口"] --> CLASS["classify_interval()"]

    CLASS -->|"DEBRIS"| DEB["err_cnt++<br/>跳过此边沿<br/>继续等下一沿"]
    CLASS -->|"SHORT / LONG"| DECODE["送入状态机"]
    CLASS -->|"AMBIGUOUS"| AMB["倾向最近值<br/>err_cnt++<br/>继续"]
    CLASS -->|"TIMEOUT"| RST["→ reset<br/>→ SEEK_PREAMBLE"]

    DEB --> NEXT["return"]
    AMB --> NEXT
    DECODE --> NEXT
    RST --> NEXT

    DECODE -->|"SEEK_PREAMBLE"| SEEK["累计 SHORT 边沿<br/>同方向/非SHORT → 计数清零"]
    DECODE -->|"RECEIVE"| RECV["逐位解 64bits<br/>err > 3 → reset"]
    DECODE -->|"VERIFY"| VERF["校验失败 → reset<br/>校验通过 → 投票"]

    SEEK -->|"≥14 → 锁定"| LOCK["状态 → RECEIVE"]
    RECV -->|"bit_idx ≥ 64"| VERF
    VERF -->|"投票通过"| OUT["on_id(id)"]
    VERF -->|"投票失败"| RST
    OUT --> RST
```

**任何异常路径最终都收敛到 `reset → SEEK_PREAMBLE`**。没有死锁态，不需要看门狗介入。

### 3.4 串口协议 (AT 指令集)

| 指令 | 响应 | 说明 |
|------|------|------|
| `AT\r\n` | `OK\r\n` | 心跳测试 |
| `AT+SCAN\r\n` | `+CARD:XXXXXXXXXX\r\n` 或 `+NOCARD\r\n` | 单次读卡 |
| `AT+AUTO=1\r\n` | `OK\r\n` | 开启自动上报 |
| `AT+AUTO=0\r\n` | `OK\r\n` | 关闭自动上报 |
| `AT+CANCEL\r\n` | `OK\r\n` | 中止当前读卡 |
| `AT+VER\r\n` | `+VER:SR3-1.0\r\n` | 固件版本 |

---

## 4. 解码状态机

```mermaid
stateDiagram-v2
    [*] --> SEEK_PREAMBLE

    state SEEK_PREAMBLE {
        [*] --> 扫描边沿
        扫描边沿 --> 分类间隔 : 收到 edge
        分类间隔 --> 检查交替 : is SHORT
        分类间隔 --> 重置计数 : not SHORT
        检查交替 --> 累加计数 : 方向交替
        检查交替 --> 重置计数 : 同方向
        累加计数 --> 锁定 : short_cnt ≥ 14
    }

    state RECEIVE {
        逐位解码 --> SHORT分支 : interval = SHORT
        逐位解码 --> LONG分支 : interval = LONG
        逐位解码 --> 碎片跳过 : interval = DEBRIS
        逐位解码 --> 超时重置 : interval = TIMEOUT

        SHORT分支 --> 得1bit : need_skip = 0 (中间跳变)
        SHORT分支 --> 丢弃 : need_skip = 1 (位边界)
        LONG分支 --> 复制上一位
    }

    得1bit --> 检查收满 : bit_idx++
    丢弃 --> 检查收满
    复制上一位 --> 检查收满
    检查收满 --> VERIFY : bit_idx ≥ 64
    检查收满 --> 逐位解码 : 继续收

    SEEK_PREAMBLE --> RECEIVE : 锁定成功 · 计算 avg_short

    state VERIFY {
        [*] --> 同步头校验 : bits[0..8] ≥ 7 个 1?
        同步头校验 --> 行校验 : 通过
        同步头校验 --> 失败 : 失败
        行校验 --> 列校验 : 10行偶校验全过
        行校验 --> 失败 : 失败
        列校验 --> 停止位 : 4列偶校验全过
        列校验 --> 失败 : 失败
        停止位 --> 提取ID : bit[63] == 0
        停止位 --> 失败 : 失败
    }

    VERIFY --> 帧投票 : 校验通过
    VERIFY --> SEEK_PREAMBLE : 校验失败

    帧投票 --> 输出卡号 : 连续 2 帧 ID 一致
    帧投票 --> SEEK_PREAMBLE : 帧不一致

    输出卡号 --> SEEK_PREAMBLE

    RECEIVE --> SEEK_PREAMBLE : TIMEOUT 或 err > 3
```

> 详细算法见 [[EM4095详解]] 第三章。

---

## 5. 源文件组织

```
01_Hardware/
├── bsp/
│   ├── inc/
│   │   ├── spsc_fifo.h          # SPSC 无锁环形 FIFO (已有)
│   │   ├── bsp_em4095.h        # EM4095 硬件抽象 + 边沿 FIFO 定义
│   │   ├── bsp_uart.h          # UART API (已有)
│   │   ├── bsp_systick.h       # SysTick API (已有)
│   │   ├── bsp_clk.h           # 时钟 API (已有)
│   │   └── bsp_config.h        # 硬件配置集中管理 (已有)
│   └── src/
│       ├── bsp_em4095.c         # ATIM3 初始化 + 捕获 ISR
│       ├── bsp_uart.c           # LPUART 驱动 + ISR (已有)
│       ├── bsp_systick.c        # SysTick ISR (已有)
│       ├── bsp_clk.c            # 时钟初始化 (已有)
│       └── bsp.c                # BSP 总初始化 (已有)
│
├── driver/                      # DDL 驱动库 (已有, 按需激活)
│   ├── atim3.h / atim3.c
│   ├── lpuart.h / lpuart.c
│   ├── gpio.h / gpio.c
│   └── ...
│
└── f002/common/                 # MCU 平台层 (已有)
    ├── HC32F002.h
    ├── system_hc32f002.c
    └── interrupts_hc32f002.c

02_Midware/
└── em4095/
    ├── em4095_core.h            # 共用类型 + 解码器 API
    └── em4095_core.c            # classify + process_edge + verify + vote

03_Application/
├── main.c                       # 主循环 + 状态机 + 指令解析
├── ddl_device.h                 # 器件选型 (已有)
├── cmd_parse.h                  # 指令解析器
└── cmd_parse.c                  # 指令解析实现
```

---

## 6. 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 采集方案 | 方案② Input Capture | ISR 仅 ~3.9kHz，硬件锁存精度 ±1µs，CPU 负载极低 |
| ISR 策略 | 只记录不判断 | < 2µs 执行时间，不丢边沿，所有算法推迟到主循环 |
| 边沿 FIFO | SPSC 无锁 | 零中断屏蔽，ISR 永远不会等锁，主循环永远不会阻塞 ISR |
| ATIM3 数字滤波 | f_DTS ÷ 32 | 硬件滤除 < 1µs 毛刺，减少假边沿中断 |
| 解码位置 | 全部主循环 | 可阻塞不丢数据（FIFO 缓冲 128 条 = 32ms） |
| 串口协议 | AT 指令文本帧 | 调试友好，`\r\n` 分隔，兼容任意串口工具 |
| 帧投票 | 连续 2 帧一致 | 平衡速度与可靠性 (EM4100 每 32ms 一帧) |
| 校验策略 | 同步头 + 行列 + 停止位 | 双重偶校验，误判概率 ≈ 1/32768 |
| 错误恢复 | 所有异常 → reset | TIMEOUT/err>3/校验失败 全部收敛到 SEEK_PREAMBLE |

---

## 7. 时序参数

| 参数 | 值 |
|------|------|
| EM4100 帧周期 | ~32.8ms (64 bits × 512µs) |
| 帧间隔 | 32~35ms |
| 边沿间隔 (Manchester) | 256µs (SHORT) / 512µs (LONG) |
| ATIM3 捕获 ISR 频率 | ~3.9kHz (每边沿一次) |
| ATIM3 ISR 执行时间 | < 2µs @48MHz |
| UART ISR 执行时间 | < 1µs @48MHz |
| edge_fifo 缓冲能力 | 128 条 ≈ 32ms（一整帧） |
| 解码延迟 (2帧投票) | ~66ms |
| 最坏响应时间 (含锁相) | ~100ms |
| UART TX 单帧 | ~5.7ms (14 字节 × 10bit ÷ 19200bps) |
