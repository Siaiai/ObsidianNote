---
领域:
  - 工作
类型:
  - 方案
项目:
  - "[[SR3读卡器]]"
任务:
  - "[[260802SR3读卡器架构]]"
相关任务:
  - "[[260802SR3读卡器需求]]"
  - "[[260802SR2读卡器行为]]"
状态: 进行
创建时间: 2026-08-02T00:24:42
截止时间: 2026-08-02T23:24:00
完成时间:
---

## 待办列表
- [ ] 整体架构
- [ ] 中断与冲突
- [ ] 业务流程闭环

---

## 1. 整体架构

### 1.1 分层架构 + 数据流

```mermaid
flowchart TD
    subgraph APP["Application · 主循环"]
        direction LR
        CMD["指令解析<br/>7条命令: 0x7A/0x75/0x9A/0x95/0x9D/0x90/0xA0"]
        SM["模式状态机<br/>模式1/2/3 · 进站0x55/下放0xAA"]
        FMT["响应格式化<br/>卡号包 8/9字节 · Fail包 7/8字节"]
        LED["LED控制<br/>红绿互斥 · 模式闪烁 · 黑卡检测"]
    end

    subgraph MID["Middleware · 主循环"]
        DEC["解码核心 em4095_core<br/>classify → process_edge → verify → vote"]
        PROTO["协议层<br/>0x10帧 · BCC计算 · 帧解析"]
    end

    subgraph BSP["BSP 驱动层"]
        subgraph FIFO["SPSC FIFO 无锁缓冲"]
            F3["edge_fifo (128条)<br/>{level, interval}"]
            F1["g_rxFifo (256B)<br/>ISR写 → 主循环读"]
            F2["g_txFifo (256B)<br/>主循环写 → ISR读"]
        end
        subgraph ISR["ISR 上下文"]
            ISR_EM["捕获 ISR<br/>读CCR → 算interval"]
            ISR_RX["RX ISR<br/>读 DR → 写 rxFifo"]
            ISR_TX["TX ISR<br/>读 txFifo → 写 DR"]
        end
        BSP_EM["bsp_em4095<br/>ATIM3 + SHD控制<br/>125kHz载波启停"]
        BSP_UART["bsp_uart<br/>UART读写 · TX非阻塞"]
        BSP_TICK["bsp_systick<br/>1ms时基 · 定时器驱动"]
    end

    subgraph HAL["HAL 硬件层"]
        ATIM3["ATIM3<br/>双边沿捕获<br/>数字滤波 f_DTS÷32"]
        LPUART["LPUART1<br/>PD05/PD06<br/>19200bps 8N1"]
        SYSTICK["SysTick · 1ms"]
        GPIO["GPIO<br/>SHD: EM4095使能<br/>LED: 红绿控制"]
    end

    subgraph EXT["External 外部"]
        TAG["EM4100 Tag<br/>125kHz 负载调制<br/>Manchester 64bit帧"]
        EM4095["EM4095<br/>125kHz RF前端<br/>PLL + 解调 + Schmitt"]
        HOST["上位机/控制器<br/>19200bps 8N1<br/>0x10帧 + 0x55帧(升级)"]
    end

    %% 读卡数据流
    TAG -.->|"125kHz RF"| EM4095
    EM4095 -->|"DEMOD_OUT 数字基带"| ATIM3
    ATIM3 -->|"捕获中断 ~3.9kHz"| ISR_EM
    ISR_EM -->|"写 {level, interval}"| F3
    F3 -->|"主循环消费"| DEC
    DEC -->|"40-bit ID (5字节)"| SM

    %% 串口RX流
    HOST -->|"RX"| LPUART
    LPUART -->|"RC中断"| ISR_RX
    ISR_RX -->|"写字节"| F1
    F1 -->|"主循环读取"| CMD
    CMD --> SM

    %% 串口TX流
    SM -->|"上报数据"| FMT
    FMT -->|"写字节"| F2
    F2 -->|"TXE中断消费"| ISR_TX
    ISR_TX -->|"写DR"| LPUART
    LPUART -->|"TX"| HOST

    %% 控制流
    SYSTICK -->|"1ms"| BSP_TICK
    SM -->|"start/stop"| BSP_EM
    BSP_EM -->|"SHD时序 + ATIM3配置"| ATIM3
    BSP_EM -->|"SHD高/低"| GPIO
    SM -->|"LED状态"| LED
    LED -->|"GPIO写"| GPIO
```

### 1.2 分层职责

| 层 | 上下文 | 做什么 | **不做什么** |
|------|--------|--------|-------------|
| ISR (捕获/UART/SysTick) | 中断 | 读寄存器 → 算差值 → 写FIFO | 不解码 · 不分类 · 不校验 · 不判断 |
| SPSC FIFO ×3 | 跨上下文 | 无锁生产者-消费者通道 | 不阻塞 · 不关中断 · 不等锁 |
| Middleware (解码核心) | 主循环 | classify → process_edge → verify → 2/3投票 | 不碰硬件寄存器 |
| Middleware (协议层) | 主循环 | 0x10帧解析 · BCC计算 · 帧组装 | 不关心解码细节 |
| Application | 主循环 | 三模式状态机 · LED · 上报策略 · 指令响应 | 不碰硬件 · 不解码 |
| Bootloader | 独立固件 | 0x55帧协议 · Flash擦写 · CRC16+XOR | 不参与日常业务 |

### 1.3 源文件组织

```
01_Hardware/
├── bsp/
│   ├── inc/
│   │   ├── spsc_fifo.h          # SPSC FIFO (已有)
│   │   ├── bsp_em4095.h        # EM4095抽象 + edge_fifo定义
│   │   ├── bsp_uart.h          # UART API (已有)
│   │   ├── bsp_systick.h       # SysTick API (已有)
│   │   ├── bsp_clk.h           # 时钟 (已有)
│   │   └── bsp_config.h        # 集中配置 (已有)
│   └── src/
│       ├── bsp_em4095.c         # SHD时序 + ATIM3初始化 + 捕获ISR
│       ├── bsp_uart.c           # LPUART + ISR (已有)
│       ├── bsp_systick.c        # SysTick ISR (已有)
│       ├── bsp_clk.c            # 时钟初始化 (已有)
│       └── bsp.c                # Bsp_Init() (已有)
├── driver/                      # DDL库 (按需激活)
│   ├── atim3.{h,c} · lpuart.{h,c} · gpio.{h,c} · sysctrl.{h,c}
│   └── ...
└── f002/common/                 # MCU平台层 (已有)
    ├── HC32F002.h · system_hc32f002.c · interrupts_hc32f002.c

02_Midware/
├── em4095/
│   ├── em4095_core.h            # 共用类型: edge_t, decoder_t, class_t, state_t
│   └── em4095_core.c            # classify + process_edge + verify + vote
└── protocol/
    ├── proto_10.h               # 0x10帧协议: 卡号包 / Fail包 / BCC
    └── proto_10.c               # 帧解析 + 帧组装

03_Application/
├── main.c                       # 主循环: Bsp_Init() → 模式状态机 → poll
├── ddl_device.h                 # 器件选型 (已有)
├── app_mode.h                   # 模式管理: Send_Manner, 上报策略, 定时器管理
├── app_mode.c                   # 模式切换 · 进站冷却 · 二次上报 · 忙保护
├── app_cmd.h                    # 7条指令处理
├── app_cmd.c                    # 0x7A/0x75/0x9A/0x95/0x9D/0x90/0xA0响应
├── app_led.h                    # LED状态机
└── app_led.c                    # 红绿互斥 · 模式闪烁 · 黑卡检测

Bootloader/                      # 独立工程, 驻0x0000~0x1BFF
├── boot_main.c                  # 上电 → CMD 0x04 → 检查UART → 跳App或驻留
├── boot_proto_55.c              # 0x55帧解析: 0x81/0x82/0x83
├── boot_flash.c                 # Flash擦写: 扇区512B, App区0x1C00起
├── boot_crc.c                   # CRC16-USB (0x8005, 初值0xFFFF)
└── boot_crypto.c                # XOR解密 (key="603337")
```

### 1.4 Flash 布局

```
0x0000 ┌──────────────────────┐
       │     Bootloader        │  ~6KB
       │     0x55帧升级协议      │
0x1C00 ├──────────────────────┤  ← APP_START_ADDR
       │     Application       │  最大 10KB
       │     读卡业务            │
0x4600 ├──────────────────────┤  ← UPD_PARAM_FLASH_ADDR
       │   Upgrade Param       │  512B (共享状态区)
0x4800 └──────────────────────┘
```

---

## 2. 中断与冲突

### 2.1 核心原则：ISR 只记录，不判断

```mermaid
flowchart TD
    subgraph isr["捕获 ISR · 执行时间 < 2µs"]
        I1["① 读 CCR 锁存值"] --> I2["② interval = cap - last_cap"]
        I2 --> I3["③ 读捕获方向 (上升→level=1, 下降→level=0)"]
        I3 --> I4["④ 写 {level, interval} 到 edge_fifo"]
    end

    subgraph main["主循环 · 无实时性约束"]
        M1["classify_interval()<br/>DEBRIS / SHORT / AMBIGUOUS / LONG / TIMEOUT"]
        M1 --> M2["process_edge()<br/>SEEK_PREAMBLE → RECEIVE → VERIFY"]
        M2 --> M3["verify_frame()<br/>同步头≥7/9 + 10行 + 4列 + 停止位"]
        M3 --> M4["vote_and_output()<br/>2/3帧投票一致 → on_id()"]
    end

    isr -->|"edge_fifo (128条)"| main

    note1["ISR 内不做: 毛刺判定 · SHORT/LONG分类 · 同步头搜索 · 校验 · 投票<br/>全部推迟到主循环 —— ISR 是无状态的 (仅 static last_cap + last_ovf)"]
```

### 2.2 SPSC FIFO 为什么不会阻塞 ISR

```
         生产者 (唯一)              消费者 (唯一)
    ┌──────────────────┐    ┌──────────────────┐
    │ ATim3_IRQHandler │    │ em4095_poll()    │
    │                  │    │                  │
    │ head 由 ISR 独占  │    │ tail 由主循环独占  │
    │ 只 write 不 read │    │ 只 read 不 write │
    └──────┬───────────┘    └──────┬───────────┘
           │                       │
           ▼                       ▼
    ┌─────────────────────────────────────────────┐
    │              edge_fifo (SPSC)                │
    │  · 两方各持一个指针 → 零互斥锁                 │
    │  · ISR 永远不会等主循环释放锁                  │
    │  · DMB 屏障保证数据对消费者立即可见             │
    │  · 满时静默丢弃 → ISR 不阻塞                  │
    └─────────────────────────────────────────────┘
```

**三者均如此：** `g_rxFifo` (ISR写→主循环读)、`g_txFifo` (主循环写→ISR读)、`edge_fifo` (ISR写→主循环读)。无一例外。

### 2.3 中断优先级与抢占分析

```
NVIC 优先级 (0=最高, 3=最低):

  IrqLevel0  [保留]
  IrqLevel1  [保留]
  IrqLevel2  ATIM3 捕获中断      ← 边沿捕获，CCR硬件锁存
             LPUART1 中断        ← RX/TX，有256B FIFO缓冲
  IrqLevel3  SysTick             ← 1ms时基
```

ATIM3 和 LPUART1 同优先级 → NVIC 按中断号顺序执行，不会互相抢占。即使 LPUART1 先执行 RX ISR (<1µs)，**ATIM3 的 CCR 寄存器已被硬件在边沿瞬间锁存**——ISR 延迟几微秒去读取，值不会丢也不会错。

### 2.4 防误码分析：逐个场景

| 场景              | 风险评估  | 架构如何消除                                                                  |
| --------------- | ----- | ----------------------------------------------------------------------- |
| ISR 执行解码被打断丢状态  | ❌ 不成立 | **ISR 不做解码**。ISR 只有 4 行代码，无状态机                                          |
| 主循环解码到一半 ISR 写入 | ❌ 不成立 | SPSC: 写方只动 head，读方只动 tail。双方操作不同指针，无竞争                                  |
| 毛刺触发假中断         | ⚠️ 可能 | **ATIM3 硬件数字滤波** (f_DTS÷32) 在边沿前滤除 <1µs 毛刺。逃逸碎片由 `classify → DEBRIS` 丢弃 |
| 解码耗时阻塞主循环       | ⚠️ 可能 | `process_edge()` 每次只处理 1 条边沿，O(1)。poll() 批量 but 每个边沿 <5µs。总耗时远小于帧间隔     |
| TX 大包发送阻塞主循环    | ❌ 不成立 | `Bsp_UartWrite()` 非阻塞——推入 txFifo 即返回。TXE 中断异步逐字节发送                      |
| 卡离开后状态机死锁       | ❌ 不成立 | 每个状态都有 TIMEOUT 出口。`interval > 2.5×base` → 触发 reset 回 SEEK_PREAMBLE      |
| EM4095 初始化时读到乱码 | ⚠️ 可能 | `bsp_em4095_start()` 先清空 edge_fifo，再使能 ATIM3 中断。SHD 启动后延迟 50ms 等 PLL 锁定 |
| 模式切换时边沿 FIFO 残留 | ⚠️ 可能 | stop → 禁中断 → 清空 edge_fifo → 重置 decoder → start。切换原子化                    |

### 2.5 时序分析：最坏情况

```
EM4100 一帧 = 128 个边沿，帧周期 ≈ 32.8ms
平均边沿间隔 ≈ 256µs (SHORT) / 512µs (LONG)

ISR 执行时间:
  · 捕获 ISR:   < 2µs  @48MHz
  · UART RX ISR: < 1µs
  · UART TX ISR: < 1µs

主循环一轮最坏耗时:
  · em4095_poll(): 消费N条边沿，每条 <5µs (classify + process_edge)
  · 指令处理 + 响应格式化: <500µs
  · LED更新: <10µs
  · 主循环单轮最坏 ≈ 1.5ms (N=50, 含UART处理)

128 条 FIFO 缓冲能力:
  · 1ms 内产生 ~4 个边沿 (256µs间隔)
  · 128 条 → 可缓冲 32ms = 整整一帧数据
  · 主循环停滞超过 32ms 才会溢出 → 实际不会发生 (无阻塞调用)
```

**结论：** 即使主循环单轮耗时 5ms（例如正在格式化输出），128条 FIFO 可吸收 32ms 的边沿数据——JS 一帧的缓冲时间。溢出窗口是主循环连续停滞 >32ms，在实际代码中不可能出现。

---

## 3. 业务流程闭环

### 3.1 模式状态机

```mermaid
stateDiagram-v2
    [*] --> INIT
    INIT --> MODE1 : 上电默认

    state MODE1 {
        [*] --> M1_IDLE
        M1_IDLE --> M1_DECODE : 卡靠近 · 解码中
        M1_DECODE --> M1_REPORT : 新卡号! (与上次不同)
        M1_REPORT --> M1_IDLE : 上报完成
        M1_DECODE --> M1_IDLE : 同卡 · 跳过
    }

    state MODE2 {
        [*] --> M2_IDLE
        M2_IDLE --> M2_DECODE : 卡靠近
        M2_DECODE --> M2_REPORT : 新卡号!
        M2_REPORT --> M2_IDLE : 上报完成 (含BCC)
    }

    state MODE3 {
        [*] --> M3_LOOP
        M3_LOOP --> M3_SEND : 100ms定时器到期 (有卡)
        M3_LOOP --> M3_SEND_FAIL : 500ms定时器到期 (无卡)
        M3_SEND --> M3_LOOP
        M3_SEND_FAIL --> M3_LOOP
    }

    MODE1 --> MODE2 : 0x9A 或 0x95
    MODE2 --> MODE1 : 0x7A 或 0x75
    MODE1 --> MODE3 : 0x9D
    MODE2 --> MODE3 : 0x9D
    MODE3 --> MODE1 : 0x7A/0x75/0x9A/0x95

    note right of MODE3
        模式3: 不等新卡事件
        定时器驱动自主上报
        红灯常亮
    end note
```

### 3.2 进站完整时序 (Send_Manner = 0x55)

```mermaid
sequenceDiagram
    participant TAG as EM4100 Tag
    participant DEC as 解码核心
    participant APP as Application
    participant UART as Host

    TAG->>DEC: ① 连续Manchester帧 (每帧~32.8ms)

    loop 解码 (SEEK→RECEIVE→VERIFY)
        DEC->>DEC: 锁定同步头 (≥14交替SHORT)
        DEC->>DEC: 收64bits → 5校验 → 2/3投票
    end

    DEC->>APP: ② on_id("AABBCCDDEE")
    APP->>APP: ③ 与上次基准比较 → 不同! → 新卡!
    APP->>APP: ④ 更新基准 = 新卡号

    rect rgb(255, 235, 238)
        Note over APP,UART: 🔴 立即上报 (不等轮询)
        APP->>UART: ⑤ 第1次: 0x10 0xFF 0x05 AA BB CC DD EE
    end

    rect rgb(255, 243, 224)
        Note over APP: 🟠 Read_card_100ms_flag=1
        Note over APP: ...等待100ms...
        APP->>UART: ⑥ 第2次: 0x10 0xFF 0x05 AA BB CC DD EE
        Note over APP: 🟡 Read_card_100ms_flag=3 (50ms冷却)
    end

    rect rgb(232, 245, 233)
        Note over APP: 🟢 冷却结束, 可响应指令
        UART->>APP: ⑦ 0x7A (上位机心跳轮询)
        APP->>UART: ⑧ 0x10 0xFF 0x04 Fail
    end
```

### 3.3 下放时序 (Send_Manner = 0xAA)

```mermaid
sequenceDiagram
    participant TAG as EM4100 Tag
    participant DEC as 解码核心
    participant APP as Application
    participant UART as Host

    TAG->>DEC: 连续Manchester帧
    DEC->>APP: on_id("AABBCCDDEE")
    APP->>APP: 与上次比较 → 不同!

    rect rgb(255, 235, 238)
        APP->>UART: 仅1次: 0x10 0xFF 0x05 AA BB CC DD EE
        Note over APP: 无二次发送 · 无冷却 · 立即可响应指令
    end

    rect rgb(232, 245, 233)
        UART->>APP: 0x75 (下放查询)
        APP->>UART: 0x10 0xFF 0x05 AA BB CC DD EE (有卡)
    end

    Note over TAG,UART: 标签离开后...
    UART->>APP: 0x75
    APP->>UART: 0x10 0xFF 0x04 Fail (无卡)
```

### 3.4 指令处理流程 (7条命令)

```mermaid
flowchart TD
    RX["UART收到完整帧"] --> PARSE["解析0x10帧: 帧头→长度→数据→BCC(可选)"]

    PARSE --> CHECK{"忙保护:<br/>Read_card_100ms_flag!=0<br/>或 B_TX1_Busy!=0 ?"}

    CHECK -->|"是, 正忙"| IGNORE["静默忽略<br/>不发任何应答"]
    CHECK -->|"否, 空闲"| DISPATCH{"CMD字节?"}

    DISPATCH -->|"0x7A"| CMD7A["① 设Send_Manner=0x55<br/>② 应答 Fail 7/8字节"]
    DISPATCH -->|"0x75"| CMD75["① 设Send_Manner=0xAA<br/>② 有卡→卡号 / 无卡→Fail"]
    DISPATCH -->|"0x9A"| CMD9A["① 切换模式2<br/>② 设Send_Manner=0x55<br/>③ 应答 Fail 8字节(含BCC)"]
    DISPATCH -->|"0x95"| CMD95["① 切换模式2<br/>② 设Send_Manner=0xAA<br/>③ 有卡→卡号9B(含BCC) / 无卡→Fail 8B"]
    DISPATCH -->|"0x9D"| CMD9D["① 切换模式3<br/>② 不应答 (进入自主上报)"]
    DISPATCH -->|"0x90"| CMD90["① 应答 Ver0310 10字节"]
    DISPATCH -->|"0xA0+0x05"| CMDA0["① NVIC_SystemReset()<br/>→ Bootloader接管"]

    IGNORE --> LOOP["继续主循环"]
    CMD7A --> LOOP
    CMD75 --> LOOP
    CMD9A --> LOOP
    CMD95 --> LOOP
    CMD9D --> LOOP
    CMD90 --> LOOP

    style IGNORE fill:#999,color:#fff
    style CHECK fill:#FF9800,color:#fff
```

### 3.5 数据包格式

```
模式1 (无BCC):
  卡号包: 0x10 0xFF 0x05 [Card0..4]           → 8字节
  Fail包: 0x10 0xFF 0x04 'F' 'a' 'i' 'l'      → 7字节

模式2/3 (含BCC):
  卡号包: 0x10 0xFF 0x05 [Card0..4] [BCC]     → 9字节, BCC=前8字节XOR
  Fail包: 0x10 0xFF 0x04 'F' 'a' 'i' 'l' [BCC]→ 8字节, BCC=前7字节XOR
```

### 3.6 LED 行为

```mermaid
flowchart LR
    subgraph 上电["上电默认"]
        UP["红灯亮 · 绿灯灭"]
    end

    subgraph 有卡["读到有效卡"]
        CARD_M1["模式1/2: 绿灯亮 · 红灯灭"]
        CARD_M3["模式3: 绿灯亮 · 红灯常亮"]
    end

    subgraph 超时["超时无卡"]
        TO_M1["模式1/2: 200ms → 绿灯灭 · 红灯亮"]
        TO_M3["模式3: 250ms → 绿灯灭"]
    end

    subgraph 黑卡["特殊标签 0x??????5A"]
        BK["两灯全灭 300ms → 两灯全亮"]
    end

    上电 --> 有卡
    有卡 --> 超时
    超时 --> 有卡
    有卡 -.->|"Card[5..7]==00,00,5A"| 黑卡
```

---

## 4. 解码状态机 (em4095_core)

```mermaid
stateDiagram-v2
    [*] --> SEEK_PREAMBLE

    state SEEK_PREAMBLE {
        [*] --> 扫描边沿
        扫描边沿 --> 分类间隔 : 收到 edge
        分类间隔 --> 检查交替 : is SHORT
        分类间隔 --> 重置计数 : not SHORT
        检查交替 --> 累加计数 : 方向交替
        检查交替 --> 重置计数 : 同方向
        累加计数 --> 锁定 : short_cnt ≥ 14
    }

    state RECEIVE {
        逐位解码 --> SHORT分支 : interval = SHORT
        逐位解码 --> LONG分支 : interval = LONG
        逐位解码 --> 碎片跳过 : interval = DEBRIS
        逐位解码 --> 超时重置 : interval = TIMEOUT
        SHORT分支 --> 得1bit : need_skip=0 (中间跳变)
        SHORT分支 --> 丢弃 : need_skip=1 (位边界)
        LONG分支 --> 复制上一位
    }

    得1bit --> 检查收满 : bit_idx++
    丢弃 --> 检查收满
    复制上一位 --> 检查收满
    检查收满 --> VERIFY : bit_idx ≥ 64
    检查收满 --> 逐位解码 : 继续收

    SEEK_PREAMBLE --> RECEIVE : 锁定成功 · 计算avg_short
    RECEIVE --> SEEK_PREAMBLE : TIMEOUT 或 err>3

    state VERIFY {
        [*] --> 停止位 : bit[63]==0?
        停止位 --> 行校验 : 10行偶校验
        停止位 --> 失败 : bit[63]≠0
        行校验 --> 列校验 : 10行全过
        行校验 --> 失败 : 任一失败
        列校验 --> 非全零 : 4列全过
        列校验 --> 失败 : 任一失败
        非全零 --> 提取ID : 卡号≠0x0000000000
        非全零 --> 失败 : 全零
    }

    VERIFY --> 帧投票 : 五道校验通过
    VERIFY --> SEEK_PREAMBLE : 任一失败

    帧投票 --> 输出卡号 : 2/3帧ID一致
    帧投票 --> SEEK_PREAMBLE : 帧不一致

    输出卡号 --> SEEK_PREAMBLE
```

> 详细算法见 [[EM4095详解]]。

---

## 5. 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 采集方案 | 方案② Input Capture (ATIM3) | ISR ~3.9kHz，硬件锁存精度±1µs，CCR防抖动延迟 |
| ISR策略 | 只记录不判断 | <2µs执行，无状态，不丢边沿 |
| 边沿FIFO | SPSC × 128条 | 零中断屏蔽，缓冲32ms=一整帧 |
| 解码位置 | 全部主循环 | ISR不判断，classify/decode/verify/vote全在主循环 |
| 解码算法 | classify 5分区 + 2/3投票 | ±20%容差，随机误判≈1/32768 |
| 校验策略 | 5关 (停止位+10行+4列+非全零+脉宽) | 比SR2多3关，减少误读 |
| 串口协议 | 0x10帧 + AT文本扩展 | 向下兼容SR2，`\r\n`兼容串口工具 |
| 帧投票 | 连续2帧一致即输出 | 平衡速度(66ms)与可靠性 |
| 上报策略 | 进站二次(100ms)+冷却(50ms)，下放单次 | 19项行为完全兼容SR2 |
| 忙保护 | Busy时静默忽略指令 | 卡号上报>指令响应，宁丢应答不丢卡 |
| EM4095初始化 | SHD=1 50ms → SHD=0 50ms | 等待PLL锁定，启动前清空FIFO |
| 模式3 | 定时器驱动，不等事件 | 有卡100ms/无卡500ms，持续自主上报 |
| 固件升级 | Bootloader独立工程 | 0x55帧，CRC16+XOR，Flash擦写+栈校验 |

---

## 6. 时序参数

| 参数 | 值 |
|------|------|
| MCU | HC32F002 · Cortex-M0+ · 48MHz |
| EM4100 帧周期 | ~32.8ms (64bits × 512µs) |
| 边沿间隔 | 256µs SHORT / 512µs LONG |
| ATIM3 捕获ISR | ~3.9kHz，执行 <2µs |
| edge_fifo 缓冲 | 128条 ≈ 32ms |
| 进站响应时间 | ~66ms (解码) + 0ms (立即上报) |
| 进站二次上报 | 100ms 后 |
| 进站冷却 | 50ms (共150ms不可响应指令) |
| 模式3 有卡间隔 | 100ms |
| 模式3 无卡间隔 | 500ms |
| UART TX 卡号包 | ~4.2ms (8B × 10bit ÷ 19200) |
| 看门狗 | IWDT ~106ms |
