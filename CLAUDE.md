# CLAUDE.md — SIA笔记规范

向这个 Obsidian 仓库新增或修改笔记时，必须遵守以下规则。完整规范见 `规范.md`。

## 禁止行为

- **禁止删除或修改任何文件，除非用户明确要求** — 这是最基本的规则，任何批量重命名、移动、删除都必须先得到用户确认
- 禁止在非笔记目录随意创建文件（`.obsidian/`、`资源/`、`.claude/`、`.git/`）
- 禁止修改 `.obsidian/` 配置目录中的任何文件，除非用户明确要求
- 禁止创建项目笔记时带 `[[ ]]` 包裹 `项目` 字段值

## 目录结构

```
{领域}/
  ├── 项目/
  │   └── {项目名}.md          ← 项目定义文件
  └── {项目名}/
      └── {笔记名}.md           ← 该项目的具体笔记
```

5个领域目录：`工作/`、`日志/`、`输出/`、`学习/`、`个人/`

### 文件命名规则

- 工作任务笔记: `YYMMDD{任务简述}.md`，如 `260707GUI增加参数组件.md`
- 日志: `YYYY-MM-DD.md`，如 `2026-01-04.md`
- 其他笔记: 自由命名，建议中文

## 属性规则（核心）

### 数组 vs 单值

- **数组格式**（每个值独立一行 `  - `）：`领域`、`类型`、`项目`、`任务`、`相关任务`、`相关项目`
- **单值格式**（`字段: 值`）：`状态`、`创建时间`、`截止时间`、`完成时间`

```yaml
# 正确：数组
领域:
  - 工作
类型:
  - 开发
项目:
  - "[[GUI]]"

# 正确：单值
状态: 完成
创建时间: 2026-07-07T13:22:00
```

### wiki 内链规则（关键，容易出错）

| 谁 | `领域` 字段写法 | `项目` 字段写法 |
|---|---|---|
| 领域定义（`领域/X.md`） | `- "[[工作]]"` | 不出现 |
| 项目文件（`X/项目/Y.md`） | `- "[[工作]]"` | `- Y`（纯文本，不加 `[[ ]]`） |
| 工作任务 | `- 工作`（纯文本） | `- "[[Y]]"`（wikilink） |
| 日志 | `- "[[日志]]"` | 不出现 |
| 输出文章 | `- 输出`（纯文本） | `- "[[Y]]"`（wikilink） |
| 学习笔记 | `- 学习`（纯文本） | `- "[[Y]]"`（wikilink） |
| 个人笔记 | `- 个人`（纯文本） | `- "[[Y]]"`（wikilink） |

**记忆口诀**：领域定义和项目笔记用 wikilink 包领域名；普通笔记用纯文本。

### 时间格式

`YYYY-MM-DDTHH:mm:ss`，**必须带秒**。模板表达式: `{{date:YYYY-MM-DDTHH:mm:ss}}`

### 空值规则

- `相关任务`、`相关项目` 无关联时：字段后直接换行留空，不要写 `- ""`
- `完成时间` 未完成时：留空行
- `截止时间`：仅工作任务有此字段，必填

## 各笔记模板速查

### 领域定义 → `领域/{领域名}.md`

```yaml
领域:
  - "[[工作]]"
类型:
  - 领域
创建时间: 2026-01-01T00:00:00
```

### 工作项目 → `工作/项目/{项目名}.md`

```yaml
领域:
  - "[[工作]]"
类型:
  - 项目
项目:
  - {项目名}            ← 纯文本！
相关项目:
状态: 规划中
创建时间: 2026-08-01T09:00:00
```

内容：dataview 查询块（见下方固定内容模板）

### 工作任务 → `工作/{项目名}/YYMMDD{任务}.md`

```yaml
领域: 工作
类型:
  - 开发
项目:
  - "[[{项目名}]]"
任务:
  - "[[{YYMMDD任务名}]]"
相关任务:
状态: 待办
创建时间: 2026-08-01T09:00:00
截止时间: 2026-08-10T17:30:00
完成时间:
```

内容：
```
## 待办列表
- [ ] 编码
- [ ] 测试
- [ ] 合并
```

### 日志 → `日志/YYYY-MM-DD.md`

```yaml
领域:
  - "[[日志]]"
类型:
  - 日记
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容：
```
## 执行清单

## 日志

## 今日总结
```

### 输出文章 → `输出/{项目名}/{文章名}.md`

```yaml
领域:
  - 输出
类型:
  - 文章
项目:
  - "[[{项目名}]]"
状态: 草稿
创建时间: 2026-08-01T09:00:00
完成时间:
```

### 学习项目 → `学习/项目/{项目名}.md`

同工作项目，但领域改为 `"[[学习]]"`，内容同 dataview 查询块。

### 学习笔记 → `学习/{项目名}/{笔记名}.md`

```yaml
领域: 学习
类型:
  - 笔记
项目:
  - "[[{项目名}]]"
状态: 待学
创建时间: 2026-08-01T09:00:00
完成时间:
```

### 个人项目 → `个人/项目/{项目名}.md`

同工作项目，但领域改为 `"[[个人]]"`，内容同 dataview 查询块。

### 个人笔记 → `个人/{项目名}/{笔记名}.md`

```yaml
领域: 个人
类型:
  - 记录
项目:
  - "[[{项目名}]]"
状态: 规划中
创建时间: 2026-08-01T09:00:00
完成时间:
```

## 项目文件的固定内容

所有项目文件（工作/学习/个人）内容统一为：

```
## 相关

```dataview
TABLE 类型, 状态, 创建时间
FROM ""
WHERE contains(项目, this.file.link)
SORT 创建时间 DESC
```
```

## 类型与状态速查

| 领域 | 可用类型 | 可用状态 |
|---|---|---|
| 工作 | 项目、开发、会议、方案、调研、文档 | 任务: 待办/进行/完成，项目: 规划中/进行中/已归档 |
| 日志 | 日记 | — |
| 输出 | 文章 | 草稿/已发布 |
| 学习 | 笔记、练习、阅读、课程 | 待学/进行/已掌握 |
| 个人 | 记录、目标、习惯 | 规划中/进行中/已归档 |

## 模板位置

- cform 表单: `资源/form-flow插件/form/`（7个: 工作任务、工作项目、学习笔记、学习项目、个人笔记、个人项目、输出文章）
- 日记模板: `资源/模板/日志模板-day.md`
- 完整规范: `规范.md`

## 资源目录说明

`资源/` 目录存放插件配置和模板，由 Obsidian 管理，无需手动维护。`附件/` 子目录由 Obsidian 自动创建和管理。
