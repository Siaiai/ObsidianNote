# 笔记规范

## 核心理念

- 5个领域：工作、日志、输出、学习、个人
- 领域 -> 项目 -> 笔记 三级结构，属性驱动
- 所有属性字段统一 YAML 数组格式（单值字段例外：状态、创建时间、截止时间、完成时间）
- 领域 wiki 内链仅领域定义和项目笔记使用，普通笔记用纯文本

## 目录结构

```
{领域}/
  ├── 项目/
  │   └── {项目名}.md
  └── {项目名}/
      └── {笔记名}.md
```

注意：输出领域不需要项目定义文件（`输出/项目/` 为空目录，不强制删除也无需在其中创建文件）。

## 时间格式

`YYYY-MM-DDTHH:mm:ss`，必须带秒，如 `2026-08-01T17:30:00`。

模板表达式: `{{date:YYYY-MM-DDTHH:mm:ss}}`

## 属性表 (10个)

| 属性 | 格式 | 说明 |
|---|---|---|
| 领域 | 数组 | 领域名（纯文本）或 [[领域名]]（wiki 内链），见 wiki 内链规则 |
| 类型 | 数组 | 见类型表 |
| 项目 | 数组 | [[项目名]] wiki 内链（项目文件自身除外，用纯文本） |
| 任务 | 数组 | [[YYMMDD任务名]] wiki 内链（仅工作任务） |
| 状态 | 单值 | 见状态表 |
| 创建时间 | 单值 | YYYY-MM-DDTHH:mm:ss |
| 截止时间 | 单值 | YYYY-MM-DDTHH:mm:ss（仅工作任务） |
| 完成时间 | 单值 | YYYY-MM-DDTHH:mm:ss |
| 相关任务 | 数组 | [[关联任务]] wiki 内链（仅工作任务，无关联时留空行） |
| 相关项目 | 数组 | [[关联项目]] wiki 内链（仅项目笔记，无关联时留空行） |

## 类型与状态

| 领域 | 类型 | 状态 |
|---|---|---|
| 工作 | 项目 / 开发 / 会议 / 方案 / 调研 / 文档 | 任务: 待办 / 进行 / 完成，项目: 规划中 / 进行中 / 已归档 |
| 日志 | 日记 | — |
| 输出 | 文章 | 草稿 / 已发布 |
| 学习 | 笔记 / 练习 / 阅读 / 课程 | 待学 / 进行 / 已掌握 |
| 个人 | 记录 / 目标 / 习惯 | 规划中 / 进行中 / 已归档 |

## wiki 内链规则

| 层级 | 领域字段 | 说明 |
|---|---|---|
| 领域定义 | `[[领域名]]` 数组 | 如 `- "[[工作]]"` |
| 项目笔记 | `[[领域名]]` 数组 | 同上 |
| 普通笔记（任务/日记/文章/学习笔记/个人笔记） | 纯文本数组 | 如 `- 工作`，不加 `[[ ]]` |

### 项目字段特殊规则

**项目文件自身**的 `项目` 字段用纯文本（不加 wikilink），如 `项目: - GUI`。
**其他所有文件**的 `项目` 字段用 wikilink，如 `项目: - "[[GUI]]"`。

### 空值规则

- `相关任务` 和 `相关项目`：无关联时留空行，不要写 `- ""` 或空数组项
- `完成时间`：未完成时留空行
- `截止时间`：工作任务必填，其他笔记无此字段

## 各领域模板

### 领域定义（路径: `领域/{领域名}.md`）

```yaml
领域:
  - "[[工作]]"
类型:
  - 领域
创建时间: 2026-01-01T00:00:00
```

### 工作项目（cform: 工作项目.cform，路径: `工作/项目/{项目名称}.md`）

```yaml
领域:
  - "[[工作]]"
类型:
  - 项目
项目:
  - {项目名称}
相关项目:
状态: 规划中
创建时间: 2026-08-01T09:00:00
```

内容固定为：

```
## 相关

```dataview
TABLE 类型, 状态, 创建时间
FROM ""
WHERE contains(项目, this.file.link)
SORT 创建时间 DESC
```
```

> 注意：`项目` 字段值不加 `[[ ]]`，为纯文本。

### 工作任务（cform: 工作任务.cform，路径: `工作/{项目}/{date:YYMMDD}{任务}.md`）

```yaml
领域: 工作
类型:
  - 开发
项目:
  - "[[{项目}]]"
任务:
  - "[[{YYMMDD任务名}]]"
相关任务:
状态: 待办
创建时间: 2026-08-01T09:00:00
截止时间: 2026-08-10T17:30:00
完成时间:
```

内容固定为：

```
## 待办列表
- [ ] 编码
- [ ] 测试
- [ ] 合并
```

### 日志（模板: 日志模板-day.md，路径: `日志/{date:YYYY-MM-DD}.md`）

```yaml
领域:
  - "[[日志]]"
类型:
  - 日记
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容固定为：

```
## 执行清单

## 日志

## 今日总结
```

### 输出文章（cform: 输出文章.cform，路径: `输出/{项目}/{文章名称}.md`）

```yaml
领域:
  - 输出
类型:
  - 文章
项目:
  - "[[{项目}]]"
状态: 草稿
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容以 `# {文章名称}` 开头，自由撰写。

### 学习项目（cform: 学习项目.cform，路径: `学习/项目/{项目名称}.md`）

```yaml
领域:
  - "[[学习]]"
类型:
  - 项目
项目:
  - {项目名称}
相关项目:
状态: 规划中
创建时间: 2026-08-01T09:00:00
```

内容固定为：

```
## 相关

```dataview
TABLE 类型, 状态, 创建时间
FROM ""
WHERE contains(项目, this.file.link)
SORT 创建时间 DESC
```
```

> 注意：`项目` 字段值不加 `[[ ]]`，为纯文本。

### 学习笔记（cform: 学习笔记.cform，路径: `学习/{项目}/{笔记名称}.md`）

```yaml
领域: 学习
类型:
  - 笔记
项目:
  - "[[{项目}]]"
状态: 待学
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容以 `# {笔记名称}` 开头，自由撰写。

### 个人项目（cform: 个人项目.cform，路径: `个人/项目/{项目名称}.md`）

```yaml
领域:
  - "[[个人]]"
类型:
  - 项目
项目:
  - {项目名称}
相关项目:
状态: 规划中
创建时间: 2026-08-01T09:00:00
```

内容固定为：

```
## 相关

```dataview
TABLE 类型, 状态, 创建时间
FROM ""
WHERE contains(项目, this.file.link)
SORT 创建时间 DESC
```
```

> 注意：`项目` 字段值不加 `[[ ]]`，为纯文本。

### 个人笔记（cform: 个人笔记.cform，路径: `个人/{项目}/{笔记名称}.md`）

```yaml
领域: 个人
类型:
  - 记录
项目:
  - "[[{项目}]]"
状态: 规划中
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容以 `# {笔记名称}` 开头，自由撰写。

## cform 字段格式规则

| 字段类型 | 模板格式 | 说明 |
|---|---|---|
| text（项目/任务等） | `"[[{{@字段}}]]"` | 手动加 `[[ ]]` |
| property_value_suggestion | `"{{@字段}}"` | 控件自动加 `[[ ]]` |
| select | `{{@字段}}` | 单选/多选值 |

## 文件清单

- 领域定义: `领域/{工作,日志,输出,学习,个人}.md`（5个）
- cform: `资源/form-flow插件/form/`（7个）
- 日记模板: `资源/模板/日志模板-day.md`
