# 笔记规范（唯一事实源）

本文件是这个 Obsidian 仓库的**唯一规范事实源**。任何 AI 工具在读写本仓库之前，必须先完整读取本文件与同目录的 `CLAUDE.md`，并按本文件规则执行。

## 禁止行为（最高优先级）

- **禁止删除或修改任何文件，除非用户明确要求** — 这是最基本的规则，任何批量重命名、移动、删除都必须先得到用户确认
- 禁止在非笔记目录随意创建文件（`.obsidian/`、`资源/`、`.claude/`、`.git/`）
- 禁止修改 `.obsidian/` 配置目录中的任何文件，除非用户明确要求
- 禁止创建项目笔记时带 `[[ ]]` 包裹 `项目` 字段值

## 核心理念

- 5 个领域：工作、日志、输出、学习、个人
- 领域 → 项目 → 笔记 三级结构，属性驱动
- 所有属性字段统一 YAML 数组格式（单值字段例外：状态、创建时间、截止时间、完成时间）
- 领域 wiki 内链仅领域定义和项目笔记使用，普通笔记用纯文本

## 目录结构

```
{领域}/
  ├── 项目/
  │   └── {项目名}.md          ← 项目定义文件
  └── {项目名}/
      └── {笔记名}.md           ← 该项目的具体笔记
```

5 个领域目录：`工作/`、`日志/`、`输出/`、`学习/`、`个人/`

注意：输出领域不需要项目定义文件（`输出/项目/` 为空目录，无需在其中创建文件）。

### 文件命名规则

- 工作任务笔记：`YYMMDD{任务简述}.md`，如 `260707GUI增加参数组件.md`
- 日志：`YYYY-MM-DD.md`，如 `2026-01-04.md`
- 其他笔记：自由命名，建议中文

## 属性规则（核心）

### 数组 vs 单值

- **数组格式**（每个值独立一行 `  - `）：`领域`、`类型`、`项目`、`任务`、`相关任务`、`相关项目`
- **单值格式**（`字段: 值`）：`状态`、`创建时间`、`截止时间`、`完成时间`

```yaml
# 正确：数组
领域:
  - 工作
类型:
  - 开发
项目:
  - "[[GUI]]"

# 正确：单值
状态: 完成
创建时间: 2026-07-07T13:22:00
```

### 属性表（10 个）

| 属性 | 格式 | 说明 |
|---|---|---|
| 领域 | 数组 | 领域名（纯文本）或 [[领域名]]（wiki 内链），见 wiki 内链规则 |
| 类型 | 数组 | 见类型表 |
| 项目 | 数组 | [[项目名]] wiki 内链（项目文件自身除外，用纯文本） |
| 任务 | 数组 | [[YYMMDD任务名]] wiki 内链（仅任务笔记） |
| 状态 | 单值 | 见状态表 |
| 创建时间 | 单值 | YYYY-MM-DDTHH:mm:ss |
| 截止时间 | 单值 | YYYY-MM-DDTHH:mm:ss（仅任务笔记） |
| 完成时间 | 单值 | YYYY-MM-DDTHH:mm:ss |
| 相关任务 | 数组 | [[关联任务]] wiki 内链（仅任务笔记，无关联时留空行） |
| 相关项目 | 数组 | [[关联项目]] wiki 内链（仅项目笔记，无关联时留空行） |

### wiki 内链规则（关键，容易出错）

| 谁 | `领域` 字段写法 | `项目` 字段写法 |
|---|---|---|
| 领域定义（`领域/X.md`） | `- "[[工作]]"` | 不出现 |
| 项目文件（`X/项目/Y.md`） | `- "[[工作]]"` | `- Y`（纯文本，不加 `[[ ]]`） |
| 任务笔记（工作/个人） | `- 工作`（纯文本） | `- "[[Y]]"`（wikilink） |
| 日志 | `- "[[日志]]"` | 不出现 |
| 输出文章 | `- 输出`（纯文本） | `- "[[Y]]"`（wikilink） |
| 学习笔记 | `- 学习`（纯文本） | `- "[[Y]]"`（wikilink） |
| 个人笔记 | `- 个人`（纯文本） | `- "[[Y]]"`（wikilink） |

**记忆口诀**：领域定义和项目笔记用 wikilink 包领域名；普通笔记（任务/日记/文章/学习笔记/个人笔记）用纯文本。

**项目字段特殊规则**：项目文件自身的 `项目` 字段用纯文本（如 `项目: - GUI`）；其他所有文件的 `项目` 字段用 wikilink（如 `项目: - "[[GUI]]"`）。

### 时间格式

`YYYY-MM-DDTHH:mm:ss`，**必须带秒**，如 `2026-08-01T17:30:00`。模板表达式：`{{date:YYYY-MM-DDTHH:mm:ss}}`

### 空值规则

- `相关任务`、`相关项目` 无关联时：字段后直接换行留空，不要写 `- ""` 或空数组项
- `完成时间` 未完成时：留空行
- `截止时间`：仅任务笔记有此字段，必填

## 类型与状态

| 领域 | 可用类型 | 可用状态 |
|---|---|---|
| 工作 | 项目、开发、会议、方案、调研、文档 | 任务: 待办/进行/完成，项目: 规划中/进行中/已归档 |
| 日志 | 日记 | — |
| 输出 | 文章 | 草稿/已发布 |
| 学习 | 笔记、练习、阅读、课程 | 待学/进行/已掌握 |
| 个人 | 项目、记录、目标、习惯、开发、会议 | 任务: 待办/进行/完成，其余: 规划中/进行中/已归档 |

## 各笔记模板速查

### 领域定义 → `领域/{领域名}.md`

```yaml
领域:
  - "[[工作]]"
类型:
  - 领域
创建时间: 2026-01-01T00:00:00
```

### 项目文件（工作/学习/个人）→ `{领域}/项目/{项目名}.md`

```yaml
领域:
  - "[[工作]]"      # 学习写 "[[学习]]"，个人写 "[[个人]]"
类型:
  - 项目
项目:
  - {项目名}         # ← 纯文本，不加 [[ ]]
相关项目:
状态: 规划中
创建时间: 2026-08-01T09:00:00
```

内容固定为：

```
## 相关

```dataview
TABLE 类型, 状态, 创建时间
FROM ""
WHERE contains(项目, this.file.link)
SORT 创建时间 DESC
```
```

### 工作任务 → `工作/{项目名}/YYMMDD{任务}.md`

```yaml
领域:
  - 工作
类型:
  - 开发
项目:
  - "[[{项目名}]]"
任务:
  - "[[{YYMMDD任务名}]]"
相关任务:
状态: 待办
创建时间: 2026-08-01T09:00:00
截止时间: 2026-08-10T17:30:00
完成时间:
```

内容固定为：

```
## 待办列表
- [ ] 编码
- [ ] 测试
- [ ] 合并
```

### 个人任务 → `个人/{项目名}/YYMMDD{任务}.md`

```yaml
领域: 个人
类型:
  - 开发
项目:
  - "[[{项目名}]]"
任务:
  - "[[{YYMMDD任务名}]]"
相关任务:
状态: 待办
创建时间: 2026-08-01T09:00:00
截止时间: 2026-08-10T17:30:00
完成时间:
```

内容固定为：

```
## 待办列表
- [ ] 编码
- [ ] 测试
- [ ] 合并
```

### 日志 → `日志/YYYY-MM-DD.md`

```yaml
领域:
  - "[[日志]]"
类型:
  - 日记
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容固定为：

```
## 执行清单

## 日志

## 今日总结
```

### 输出文章 → `输出/{项目名}/{文章名}.md`

```yaml
领域:
  - 输出
类型:
  - 文章
项目:
  - "[[{项目名}]]"
状态: 草稿
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容以 `# {文章名}` 开头，自由撰写。

### 学习笔记 → `学习/{项目名}/{笔记名}.md`

```yaml
领域: 学习
类型:
  - 笔记
项目:
  - "[[{项目名}]]"
状态: 待学
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容以 `# {笔记名}` 开头，自由撰写。

### 个人笔记 → `个人/{项目名}/{笔记名}.md`

```yaml
领域: 个人
类型:
  - 记录
项目:
  - "[[{项目名}]]"
状态: 规划中
创建时间: 2026-08-01T09:00:00
完成时间:
```

内容以 `# {笔记名}` 开头，自由撰写。

## 操作规范（AI 读写本仓库时）

- **新建任务笔记**：严格按上文模板（frontmatter 齐全 + 待办列表），放 `{领域}/{项目名}/`，命名 `YYMMDD{任务}.md`
- **完成任务**：勾选待办项、`状态` 从 进行 → 完成、填写 `完成时间`（带秒）
- **产出设计文档/文章**：frontmatter 的 `项目: "[[项目名]]"` 必须正确，dataview 会自动汇总，项目文件本身无需手动修改
- **项目文件本身只是查询视图**，基本不用动

## cform 字段格式规则

| 字段类型 | 模板格式 | 说明 |
|---|---|---|
| text（项目/任务等） | `"[[{{@字段}}]]"` | 手动加 `[[ ]]` |
| property_value_suggestion | `"{{@字段}}"` | 控件自动加 `[[ ]]` |
| select | `{{@字段}}` | 单选/多选值 |

## 模板位置

- cform 表单：`资源/form-flow插件/form/`（8 个：工作任务、工作项目、学习笔记、学习项目、个人笔记、个人项目、个人任务、输出文章）
- 日记模板：`资源/模板/日志模板-day.md`

## 资源目录说明

`资源/` 目录存放插件配置和模板，由 Obsidian 管理，无需手动维护。`附件/` 子目录由 Obsidian 自动创建和管理。